# Native Codex root-to-start-directory instruction budget. Lower, never raise.
# check-shared-instructions enforces a 4 KiB reserve on every repo chain.
# Measured 2026-09-26: largest chain 65,716 B (wizard); 72 KiB also leaves room
# for the 2,355 B global file and CRLF. Evidence: docs/metrics/2026-09-26-codex-instruction-loading.md.
project_doc_max_bytes = 73728
