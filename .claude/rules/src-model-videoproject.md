---
paths:
  - "src/model/videoProject.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `model/video-persistence-mirrors-current-slot-autosave`: Video persistence MIRRORS `project.ts` and `packets.ts`: one current slot on autosave plus a saved list with soft-delete tombstones. The autosave returns false on quota rather than throwing, so the shell can WARN - video assets are big enough that a quiet failure would lose real work.
