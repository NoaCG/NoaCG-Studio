---
paths:
  - "src/model/styleVocabulary.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `model/holds-words-ranges-both-style-surfaces`: `styleVocabulary.ts` holds the WORDS and ranges both style surfaces render: the role label for each `:root` variable (a user-facing name, never CSS jargon), the group it belongs to, which tokens are lengths and over what range, the shadow presets, and the two size ladders. The wizard and the editor read them from here; neither writes its own.
