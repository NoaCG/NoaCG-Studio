---
paths:
  - "src/components/ControlPanel.tsx"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `components/build-editor-operator-view-engine-drives`: Build the in-editor operator view from the `control/` engine with `live` on: it drives the preview through `store.sendControl`, renders the machine's EVENT BUTTONS from `controlModel` (labels, sections and payloads from `machine.controls`, payload values from sample data through `store.sendEvent`), and greys them with `isEventLegal` against `store.machineGroups` exactly as a hosted control page greys them. An `adjust` press writes its new figure back into the sample data.
- **rule** `components/keep-panel-productions-block-slim-create`: Keep the panel's Productions block SLIM: create or pick a production, add the current graphic, and a link to its page. The layer stack, export, publishing and the links all live on `ProductionPage` alone, so two surfaces cannot drift.
