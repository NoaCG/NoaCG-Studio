---
paths:
  - "src/model/brand.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `model/brand-saved-look-points-instead-anonymous`: A brand IS a saved look, so `defaultBrandId` POINTS at one instead of the anonymous singleton every wizard Create used to overwrite, and `loadBrand()` lives in `packets.ts` beside the store it reads - put it here and the two modules form an import cycle the dependency gate refuses. The legacy single-brand key is READ by the app (`legacyBrandOffer`, and `captureLookFromTemplate`'s style-family fallback) and written by NOTHING but the sync seam's put.
