---
paths:
  - "src/components/wizard/kitPlan.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `wizard/make-apply-style-all-deterministic-transform`: Make "Apply this Style to all" a deterministic transform over the `:root` contract and nothing else, confirmed before it runs: `kitLookPatch` carries a motion preset only where the target design DECLARES it and keeps each graphic's text and placement. The chosen brand reaches every graphic of the set, and every kit rail entry is a jump target because the set is always built.
