---
paths:
  - "src/model/videoInputInfer.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `model/code-decides-what-editable-reads-composition`: The CODE decides what is editable: `videoInputInfer.ts` reads a composition's inputs OUT OF the module - every field read with a literal fallback, typed from that fallback - so a hand-written field gets the same control the AI would have declared. `contentInputs(declared, tsx)` is what the Content panel shows: declared inputs FIRST and unchanged, because they carry labels, options and bounds a fallback cannot express, then whatever else the code reads. A read with NO literal fallback is ignored on purpose - without a default there is nothing to show, reset to, or type the control from.
