---
paths:
  - "src/blocks/cssLength.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **trap** `blocks/reads-first-number-out-token-value`: `cssLength.ts` reads the FIRST number out of a token value and writes a new one back between the same prefix and suffix, so `calc(16px * var(--scale))` keeps its multiplier and `blur(18px)` keeps its function. A value with no number (`none`) returns null and is left alone - that is a real state, not a zero.
