---
paths:
  - "src/components/AppShell.tsx"
  - "src/components/WorkspaceDock.tsx"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `components/lay-workspace-out-fixed-centre-canvas`: Lay the workspace out from `model/layout.ts`: a fixed centre - the canvas over the timeline, split by a draggable divider - flanked by LEFT, RIGHT and BOTTOM docks, each hosting any of `code`, `inspector`, `data`, `control`, `style`, `assets`, `ai` and `export` as tabs, and each rendering only when it holds panels. `WorkspaceDock` draws a dock, AppShell `renderPanel` supplies the bodies with the tool panels wrapped in `.panel-body`, and MOBILE keeps the fused preview column plus SidePanel.
