---
paths:
  - "src/model/generationSpec.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `model/more-control-panel-user-authored-lives`: The AI More-control panel's user-authored `GenerationSpec` lives HERE rather than in `src/ai`, because `SavedProject` and `GraphicDoc` persist it as `aiSpec` (additive optional) and the model layer imports nothing above it. The category REGISTRY that interprets a spec is `src/ai/spec/categories.ts`. `normalizeSpec` is the migrate-on-read guard: an unknown version degrades to no spec, never a crash.
