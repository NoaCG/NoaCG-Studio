---
paths:
  - "src/model/csv.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `model/lands-read-table-ordinary-editable-dataset`: `importShowDataset` in `shows.ts` lands a read table as an ordinary editable dataset. There is deliberately NO link back to the file: an import is a one-way act, not a subscription.
