---
paths:
  - "src/model/types.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `model/carries-html-plus-css-plus-plus`: `types.ts` carries `SpxTemplate` - html plus css plus js plus the parsed definition, the CANONICAL unit - along with `AssetFile`, `DEFAULT_SETTINGS` and compatibility re-exports from `projectFormat.ts`.
