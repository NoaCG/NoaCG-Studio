---
paths:
  - ".agent-workflows/orchestrator/**"
  - "scripts/resume-dispatch*"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **trap** `orchestrator/keep-resume-validation-durable-claiming-dispatch`: Keep resume validation, durable claiming and dispatch in one fail-closed command. Parse saved deadlines as explicit UTC strings, preserve accepted claims through uncertain failures, and never follow a refused claim with a separate worker launch.
