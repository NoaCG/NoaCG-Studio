---
paths:
  - "src/model/videoLayout.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `model/video-shell-keeps-own-layout-prefs`: The video shell keeps its OWN layout prefs, separate from `layout.ts` on purpose: it has a simple code-and-preview split, not the SPX dockable workspace, so it stores only the split ratio and whether the code pane is collapsed.
