---
paths:
  - "src/components/AppShell.tsx"
  - "src/components/home/GraphicControlPage.tsx"
  - "src/components/video/VideoAppShell.tsx"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **invariant** `components/open-every-header-same-trio-brand`: Open every header with the same trio - brand lockup, Home, then the new-graphic door - all LEFT of the bar's `.spacer` and none of them wearing `primary`, because amber is the on-air accent. WHICH control is Home differs by surface, the crumb on Home and the logo on the dashboard, and that is not drift; `e2e/project.spec.ts` pins the order and the before-the-spacer position on all five surfaces.
