---
paths:
  - "src/components/InsertTemplateDialog.tsx"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `components/offer-assets-header-template-graphic-door`: Offer the Assets header's template-graphic door as `InsertTemplateDialog`, the catalog browser in INSERT mode over `blocks/templateInsert.ts`, asking exactly two things: PLACEMENT (from the start, or as a new next step) and whether its lines reveal together or step by step. Both answers are code-derived from one donor build per card, so a template needing its own runtime is greyed WITH the reason.
