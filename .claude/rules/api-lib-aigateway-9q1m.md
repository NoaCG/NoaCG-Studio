---
paths:
  - "api/_lib/aiGateway.ts"
  - "api/_lib/aiLiteProfile.ts"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **rule** `ai/pin-managed-calls-preserve-task-requires`: Pin `disallowPromptTraining` on for managed calls and preserve `zeroDataRetention` when the task requires it; fail closed with `zdr_unavailable` rather than silently weakening retention. Bound funded costs through approved-catalog pricing and task booking because the gateway has no per-request price cap.
