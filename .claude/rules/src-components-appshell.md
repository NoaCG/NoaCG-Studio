---
paths:
  - "src/components/AppShell.tsx"
---
<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Edit the rule files, then run npm run contracts:compile. -->

- **trap** `components/defer-inspector-reveal-past-double-click`: Defer the Inspector reveal past the double-click window: a new `selectedParts` selection arms a timer, any new pointer press cancels it, and a live canvas gesture (`canvasGestureActive`) skips it at fire time, so the workspace never resizes between the two clicks of a text double-click or under a drag. `e2e/inline-edit.spec.ts` pins it, and the reveal is desktop-only.
- **trap** `components/treat-reveal-signal-rather-than-state`: Treat `activePanel` as a REVEAL signal rather than as state: the docks key the reveal on `panelRevealNonce`, which every `setActivePanel` call bumps, so re-requesting the stored default still reveals and mount never does. Keying on the value alone clobbers the layout's own active tab on any unrelated re-render.
- **rule** `components/bind-undo-redo-globally-appshell-ctrl`: Bind undo and redo globally in AppShell - Ctrl/Cmd+Z to `undo()`, Ctrl/Cmd+Shift+Z and Ctrl+Y to `redo()` - and stand down when focus is in Monaco or a form field, or while `modalOpen()` is true. A dialog's Ctrl+Z belongs to the dialog, never to the document behind it.
- **rule** `components/open-inspector-tool-panels-right-dock`: Open with `DEFAULT_LAYOUT`: the Inspector and the tool panels in the RIGHT dock, the LEFT and BOTTOM docks EMPTY, and the `code` panel CLOSED, because the code view is optional and a Monaco pane across a third of the window tells a first-time reader the opposite. An absent panel is what closed MEANS - `loadLayout` never re-adds one - so the choice persists; a stored version-2 layout is migrated by adding `assets`, and any other version falls back to the default.
