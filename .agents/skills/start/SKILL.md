---
name: start
description: Warm a fresh session from the root instructions, then say "Ready."
---

Read `.agent-workflows/start.md` (relative to the repo root) now and follow it.
In Codex, its root read is already satisfied when the complete root AGENTS.md is in native
startup context: use that text instead of fetching the same file again. Otherwise read it.
