---
kind: owner-action
date: 2026-09-09
needs: identity
serves: now
---
# One message to the Yle contact: the network screenshot, and which OGraf renderer they run

The 2026-08-20 demo failed on Yle's network and nobody has asked them for the one screenshot that
says why. It is the gap that can sink the 25th (`docs/DEMO_2026-09-25.md` §7 row 1), and only you
hold the contact, which is why it is yours and not a session's.

## The route, under a minute

Send the Yle contact one message asking for two things:

1. Open <https://noacg.studio/app?diag=1> on the Yle network and send back a screenshot. The page
   is plain HTML that runs even when the app's own files are blocked, so the screenshot answers
   which local policy stopped the app last time.
2. Which OGraf renderer they run, if any, so the OGraf beat (`docs/DEMO_2026-09-25.md` A7) is
   prepared on their renderer rather than on ours.

**What to look at.** If the screenshot cannot be had before the 25th, say so here and the session
runs on our own connection, announced in the room. Delete this file when the message has an answer.
