# Fresh-session prompt: R1.1a base edits and tools

Implement only R1.1a of the NoaCG editor rebuild, then hand it back for review.

Purpose: let users refine actual Illustrator/SVG imports and customize catalog graphics
through a professional canvas without losing source, fields or existing motion. After
Effects and OGraf Studio are interaction/quality references. Use professional judgment
for ordinary gestures and catch routine defects yourself.

## Start and prerequisites

Fetch origin and create a dedicated implementation branch/worktree from updated
origin/main after this handoff update lands. Main must contain PR #331 (cfb28e74) and
this file. Do not start from the old planning or implementation branch tips.
Read root and applicable nested AGENTS.md. Inspect current state and any newer owner
feedback, then record a short slice brief: task, adapters, operations, affected modules,
failure/rollback cases, acceptance IDs and closing tests.

R1.0 is merged and live, with a permanent canvas, shared selection, read-only layer bars,
scrubbing, document/history operations and revision-aware preview. The wizard homepage
has Open editor Alpha: https://noacg.studio/app?editor=foundation#/editor-foundation.
Its local integration passed 45 tests; PR CI passed 842 browser tests; merge CI and
live desktop/phone viewport checks passed. Owner usability acceptance remains open.
The missing-link/old-editor report preceded deployment; engineering verified its fix.
No later owner usability verdict was supplied. The original blank-stage report is not
universally resolved. This handoff does not close that issue or later human/host gates.

## Read and inspect

- docs/EDITOR_PLAN.md and docs/EDITOR_REBUILD_PLAN.md: scope and mechanisms.
- docs/research/editor-review-brief-2026-09-18.md and
  docs/research/editor-acceptance-register-2026-09-17.md: ordered slices and B/D gates.
- docs/research/editor-workflow-review-2026-09-19/README.md: destination decisions.
- docs/research/editor-r1-foundation/README.md and implementation.md: actual evidence,
  limitations, file-level inventory and ownership.
- docs/STARTER_COLLECTIONS_PLAN.md and docs/SAVED_CONTENT_MODEL.md: integration boundaries.
- Actual screenshots under docs/research/editor-r1-foundation/: wizard-live.png,
  editor-live-desktop.png, editor-live-phone.png, catalog-built-1366.png and svg-built-1920.png.
  Compare planning references workflow-dark-1366.png / workflow-dark-1920.png in
  docs/research/editor-workflow-review-2026-09-19/; those show later features too.

## Build

Add optional wizard Finish -> Edit using the final applyDraftProject result. Keep direct
production primary; opening Edit creates no rundown item and requires no Advanced mode.
Implement base Position/Layout offset, Text/Rectangle/Ellipse creation and basic scale
handles/numeric controls. New text has a real operator field; point text and text boxes,
constrained square/circle creation and text-box reflow obey the acceptance contract.

SpxTemplate stays canonical. Extend the shared operation registry and source adapters;
gestures use fast transient previews, then one deterministic patch and one undo transaction.
Keep existing motion, source sentinels, fields, assets and sample data. Preserve the live
Alpha entry and phone viewing. No Monaco, key authoring, bar moves/trim, Set Out, new
playback, easing, grouping or default-editor switch in this slice.

Main seams in src/components/editorFoundation/: Canvas.tsx, Inspector.tsx,
EditorFoundation.tsx, PreviewController.ts,
operations.ts, session.ts, documentAdapter.ts, protocol.ts and runtime.ts;
src/blocks/{designLayout,designFields,edit,animData,animEdit}.ts;
src/components/wizard/CreationWizard.tsx, src/model/wizard.ts,
src/templates/importedDesign/svg.ts, src/store/templateStore.ts and existing save/export adapters.
Use the inventory's exact files; do not duplicate source writers or save dialogs.
Reuse e2e/fixtures/illustrator-lower-third.svg, the catalog/SVG/F4 fixtures in the
foundation evidence folder, and scripts/editor-foundation-bench.mjs.

## Close before handback

- B01: both Finish routes on actual catalog, imported SVG, quiz, timer and stretch-layout
  results; preserve generated fields/actions/followers and production behavior.
- B03/D03 core: +40px Layout offset on a flow catalog line leaves siblings and motion keys
  unchanged. Placed/SVG parent coordinates inverse-map correctly, including parent
  translate(100,80)/rotate(30)/scale(2). Numeric/canvas agreement, zoom, scale, Shift/Alt,
  Escape, atomic undo/redo, save/reopen and relevant exported output must agree.
- B04 core: create each shape/text kind, constrained dimensions, real text field updates,
  bar/selection creation, text-box reflow, cancellation and source-preserving round-trip.
  R1.1d broadens nested/unnamed identity fixtures; it cannot defer correctness used here.
- Add mapped Playwright tests; retain editor-foundation, editor-alpha-entry, wizard
  entry/shell/Finish and save/export regressions. Queue browser work via npm run queue.
  Run npm run build and the affected suite (integration plan from the fork point if main
  was integrated). Inspect real renders at 1920x1080, 1366x768 and 125% zoom; retain phone access.
- Repeat the F4 30-layer/300-key harness for actual drag feedback, not just selection/scrub:
  selection <=100ms, continuous feedback >=30Hz, no freeze >100ms, pointer-up feedback <=150ms.
  Preserve revision/asset-byte/stale-reply and single-history-transaction tests.

Commit the verified bounded slice. Update roadmap/acceptance with only measured passes,
open gates and owner feedback; add its own owner-queue route, walkthrough, screenshots,
test receipts and limitations. Stop before R1.1b; do not claim owner or real-host acceptance
from automated checks.
