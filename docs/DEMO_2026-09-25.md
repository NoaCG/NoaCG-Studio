# The 2026-09-25 session - the script, and the gap list it is

**What this is.** The ordered beats of the 25 September session: students and YLE people trying
NoaCG, hands on. For every beat: who drives, the exact route in the product, and the evidence that
it works today or the word GAP with the next step named. §7 lists every beat that is not WORKS,
with what closing it costs and who closes it; the two weeks before the date are planned from that
list. It is written to be read cold by the owner on a phone, and by a session on the morning of
the 25th.

**The owner's brief** is the 2026-09-09 ruling in `docs/OWNER_RULINGS.md`, paraphrased from
speech, so the intent binds and the wording does not. The 12th is no longer the pressure. The
date is the 25th. We owe one presentation and one step-by-step guide over two capabilities: SVG
import ending in their own graphics playing on their systems, and the CLI taking a graphic from
Codex or Claude Code to the NoaCG player in minutes.

**Status legend, used in every table.** `docs/PROMISE_AUDIT.md` grades what the product may
claim; this column grades a BEAT of this session, which is a different question: can it be run on
the day, and has a person seen it run.

| word | meaning | who closes it |
|---|---|---|
| WORKS | on `main`, pinned by a spec, and a person has driven it on the surface the beat uses | nobody |
| UNSEEN (eyes) | on `main` and pinned; the owner has not looked at it on this surface since the code last moved | a walk (`docs/acceptance/OWNER_QUEUE.md`, `kind: walk`) |
| UNSEEN (box) | on `main` and pinned; never run on the real playout hardware the beat names | a box (`kind: hardware`) |
| GAP | not there, or nothing evidences it | the row in §7 says |

Every beat table has the same six columns: number, beat, who drives, route, evidence, status.
On a phone the tables read one column at a time, which is why the who column is never pushed
into a heading. **The status column is hand-maintained.** Nothing in the repo rewrites it when an
acceptance box is ticked or an owner-queue file is deleted, so each UNSEEN and GAP row names, in
§7, the mechanism that really settles it, and §8 says who edits the cell.

---

## 0. The shape, and the six calls it rests on

The brief says "trying NoaCG", so this is a follow-along session and not a demonstration. That
one word decides most of the shape. The owner-queue item for this script carries each call with
the sentence that reverts it.

1. **They drive.** Every beat a participant can do on their own laptop is theirs. We drive only
   the beats that need a playout box or a coding-agent subscription some of them will not have.
2. **Both roads end at the same place, so it is taught once.** An imported SVG and an agent-made
   graphic both become a library record, both go into a production, and both air through one
   output URL. Road 1 and road 2 stop at the library; §5 takes the production to air once, for
   both.
3. **Road 1 goes first.** It needs no terminal, no subscription and no account until the
   production is published, so the whole room can do it. Road 2 needs Node and, for its full
   form, an agent subscription.
4. **On the day, air stops at the NoaCG player in the cloud** (owner, 2026-09-10). A graphic is
   imported and played on our own hosted player, and seeing it work there is the proof the room
   gets. No OBS on student laptops, no CasparCG box, no playout configuration of any kind. Real
   playout is a separate studio session later. The bring-your-box beats stay written in §5 so one
   can be run if somebody asks, but none is on the clock and none gates the day. This replaces
   the earlier call, which prepared four targets and chose between them on the day.
5. **The step-by-step guide is `/docs`, not a second document.** A guide that lives beside the
   product drifts, and the encore under §3 names a pair that already do. What is printed is a
   one-page index into the pages, in beat order.
   **Two versions of that page** (owner, 2026-09-10): one for the students, written to be handed
   out on the day and shared afterwards as a file, and one for the owner, which is the running
   order with the beats and who drives each. Same page, two audiences; G2 builds both.
6. **The room draws in small groups, and finishes at home** (owner, 2026-09-10). The drawing is
   done in Illustrator, in small groups rather than one file per student. Each group takes away
   two pieces to finish in their own time and send in: **a lower-third quiz template and a
   scoreboard**. What happens inside the session is deliberately smaller - one simple graphic, a
   lower third, imported and played - because the session proves the road works and the homework
   is where the artwork gets made.

**Two sibling rows of the 2026-09-09 day wave were running while this was written**, and both
landed the same day. `claude/a-cli-minutes-to-air` (pull request 190) timed the CLI path from an
empty folder to a graphic on air; its number is in R2.5. `claude/d-import-road-guide` (pull
request 191) walked the SVG import road as a stranger and wrote the end-to-end `/docs` page; it is
`/docs#first-graphic`, in G1. The deck, `docs/presentation-2026-09-25/NoaCG-2026-09-25.pptx`,
was built from this file after both had landed (O2).

**The 2026-09-12 student production** (quiz and scoreboard, drawn by students) still happens. It is
the dress rehearsal for road 1, and every UNSEEN (box) row in §5 gets its first tick there or not
at all before the 25th.

**Timing.** About ninety minutes, re-cut 2026-09-10 for calls 4 and 6: 10 opening, 20 road 1 (a
simple lower third, imported and played on the NoaCG player), 25 drawing in small groups, 25 road
2, 10 on air and the close. Air is short now because it ends at our own player instead of at four
targets, and the time that frees goes to the groups. Whoever runs the clock cuts a beat that runs
long at the next beat's boundary and never stretches it.

---

## 1. Before the day

These are not shown. They gate whether the day can happen at all, which is why they come first.

| # | beat | who | route | evidence | status |
|---|---|---|---|---|---|
| B0 | The network the session runs on lets the app boot | owner | `https://noacg.studio/app?diag=1` opened ON that network, screenshot sent back | The 2026-08-20 Yle demo failed on Yle's network. The diag page and the hydration timeout shipped the same day (`e2e/network-resilience.spec.ts`) so the screenshot would say which policy it was. **Nobody has asked the Yle contact for it since.** | **GAP.** Only the owner holds the Yle contact, so it is his (`needs: identity`): `docs/acceptance/owner-queue/2026-09-09-g-yle-network-diag-screenshot.md`. If the screenshot cannot be had, we bring our own connection (a phone hotspot) and say so in the room. |
| B1 | What is deployed is what we rehearsed | any session, the morning of | `https://noacg.studio/version.json` `commit` against the tip of `main` | `scripts/write-version.mjs` writes the stamp and `.github/workflows/deploy-verify.yml` checks it; a docs-only landing does not redeploy, a `src/` landing must match within minutes | WORKS. Run it after the last landing on the 24th and again on the 25th. |
| B2 | Accounts | participants | `/app` sign-up. Import, edit and export need none; publishing a production and `noacg save` need a free one | `docs/PROMISE_AUDIT.md`: "No account to create, edit and export" is WORKS NOW; "A production ... one persistent output URL" and "The NoaCG CLI ..." are WORKS WITH A STATED LIMITATION, the account. Email confirmation is OFF, verified live 2026-08-24 (`docs/DEPLOYMENT.md`, "Email confirmation is off"; `supabase/config.toml` `enable_confirmations = false`), so a sign-up works at once. | WORKS. One thing lies about it: the sign-up dialog still says "Check your email to confirm your account" (`src/components/auth/SignInDialog.tsx`), which on the day sends students to an inbox for nothing. Filed: `docs/backlog/sign-up-says-check-your-email-with-confirmations-off.md`. Tell the room to ignore that line until it is fixed. |
| B3 | Machines for road 2 | participants | Node 20+, Chrome or Edge, and Claude Code or Codex with a subscription (`/docs#claude-code`) | the docs page states the requirements | WORKS. The product has no gap here; the ROOM does, because a student without a subscription cannot follow R2.2 onward with an agent. R2.1 exists so that everyone still does the CLI road. |
| B4 | Hardware in the room | owner | a CasparCG 2.3.x box (the teaching install), OBS on laptops, an OGraf renderer if Yle brings one | - | owner-supplied |
| B5 | The install lines on a fresh machine | one row, now | the two Claude Code lines and the two Codex lines from `/docs#agent-install`, on a clean OS user | `docs/PROMISE_AUDIT.md`, "Claude Code and Codex plugin install lines": last executed 2026-08-22 (Claude Code) and 2026-08-27 (Codex). `docs/AGENT_CLI.md` says the `noacg-mcp` split is "not yet re-verified on Codex after the split". `@noacg/cli` 0.3.0 has been on npm since 2026-09-05. **This row is the only place those dates live.** | **GAP.** Nothing here waited on the CLI timing row; only the re-run after a CLI version bump does. |

---

## 2. Opening

| # | beat | who | route | evidence | status |
|---|---|---|---|---|---|
| O1 | What NoaCG is: free and open source, the code is the graphic, export to anything | owner | `https://noacg.studio` | The landing page may not claim more than `docs/PROMISE_AUDIT.md` grades (2026-09-08). Every sentence on it has an evidence row. | WORKS |
| O2 | The two roads and where they meet, as one picture: your artwork or your agent, then a library, then a production, then one URL. This is the deck, and the deck is this one picture plus the beat headings | owner | `docs/presentation-2026-09-25/NoaCG-2026-09-25.pptx`, slide 3 | The deck: seven slides in a `.pptx` that opens, presents and hand-edits in PowerPoint or LibreOffice with no network, built 2026-09-09 from this file once the two sibling rows had landed. Every slide sentence traces to a beat's evidence column here, and each slide's speaker notes name the beat ids, their status and the proving file. The picture is slide 3, in native shapes. `docs/README.md` names the deck; `make-deck.mjs` beside it is a starting-point generator that refuses to write over the file, so a hand edit always wins. | **UNSEEN (eyes).** The owner has not opened it; his walk is the owner-queue item filed with the deck. §7 row 13. |

---

## 3. Road 1 - your own graphic

The owner draws nothing here. Participants either bring an Illustrator, Figma or Inkscape file
made to the rules on `/docs#svg-rules`, or take a sample. The shipped samples are the safe path
and the script uses them; a participant's own file is the interesting path and the one the
stranger's walk of 2026-09-09 (pull request 191, the `#first-graphic` guide) measured.

**Two different pieces of drawing, and they must not be confused** (§0 call 6). What the room
imports and plays inside the session is ONE SIMPLE GRAPHIC - a lower third - because the session
is proving that the road works end to end. The real artwork is the take-home: small groups draw
**a lower-third quiz template and a scoreboard** in Illustrator, start them here, finish them in
their own time and send them in. R1.7 is that hand-off, and it is the beat the session is
actually for.

| # | beat | who | route | evidence | status |
|---|---|---|---|---|---|
| R1.1 | Draw it, or take a sample | participants | The five rules on `/docs#svg-rules` (cited by anchor on purpose, because the page is the one copy of them), then `/docs#svg-export` for the per-app export settings. Samples: `docs/svg-samples/lower-third.svg`, `scorebug.svg`, `quiz-board.svg` (the README there says what each teaches) | `docs.html` `#svg` is pinned by `e2e/docs.spec.ts`; 23 sample files. For a participant's own file: the exporter corpus is 47 fixtures shaped the way Illustrator, Figma, Inkscape, Affinity, Sketch, CorelDRAW and SVGO export, walked through the real door by `e2e/import-svg-corpus.spec.ts`; the 2026-08-29 sweep of 34 read 23 pass, 11 partial, **0 fail, nothing refused that should have imported**. | WORKS |
| R1.2 | Drop it. The layer names become the field names, every text layer is on | participants | `/app` -> **New graphic** -> **Import graphic** -> drop the file -> the Fields step | `e2e/import-svg.spec.ts`: "the drop is recognised, inventoried, and swaps the walk to the SVG rail"; "mapping - labels from layer names, all on by default, edits carried to the template". Owner walks of this step: 2026-08-25, 08-28, 08-29, 09-02, 09-03, each landing fixes (`docs/SVG_IMPORT_PLAN.md` §3, `docs/TEXT_BOX_BINDING.md`). | WORKS |
| R1.3 | Type a name longer than the design was drawn for. The banner gets wider, then the text wraps, and only then does it get smaller; nothing paints outside the panel | participants | on the Fields step, retype the sample of the name row and watch the preview | `e2e/import-svg.spec.ts`: "a lower third climbs the ladder in order - wider, then onto a new line, and only then smaller"; "the shipped Illustrator lower third arrives growing, not shrinking"; "with NOTHING chosen, a long name grows the banner". The order is the owner's ruling of 2026-08-26. | WORKS on the shipped samples. On a participant's own file the measured default disagrees with the designer's intent for some corpus shapes (`docs/backlog/svg-growth-default-across-exporters.md`, the `GROWTH_FINDINGS` exclusions). The guide's line for that case is one dropdown, "when the text is too long", and the script says it out loud. |
| R1.4 | Behaviour on artwork nobody at NoaCG drew. `quiz-board.svg`: the quiz is proposed from the layer names; on the production page the operator picks, locks, reveals. `scorebug.svg`: the numbers become ± steppers | participants | Fields step -> the Behaviour section (already filled for the samples) -> Create -> production page | `e2e/import-svg-behaviour.spec.ts`: "imported quiz: drawn layers are proposed from their names, and the operator drives select -> lock -> reveal"; "imported scoreboard: a numeric layer is a ± stepper that acts on air, and survives a reload"; both also driven from a CasparCG package's standalone panel. `docs/GRAPHIC_BEHAVIOUR_PLAN.md` §10. The owner's own Illustrator quiz board (2026-09-02) found three text-box defects; all three are fixed (`docs/TEXT_BOX_BINDING.md`). | **UNSEEN (eyes).** The owner has not looked at his OWN board since the three fixes (`docs/PROMISE_AUDIT.md`, "Behaviour on your own artwork", "owner walk still owed"). The 12th is that walk. |
| R1.5 | Fonts: the Fields step's Typefaces rows offer the Google door where Google has the family, or an upload, and the Finish summary names a typeface that will not travel | participants | the Fields step's typeface rows; the Finish step's notice | `e2e/import-svg.spec.ts`: "the Finish step names a typeface that will not travel"; "the Google door is offered only for a family Google actually has"; "a PostScript font name finds the bundled face". | WORKS. A Yle designer's licensed face takes the upload road; say so before they ask. |
| R1.6 | Finish: name it, then "Add to the production - go live". **Never send the room to "Create project"**: that footer button stands beside the Design, Fields and Animation steps (not Start, not Finish) and it does not save (`create()` calls `applyDraftProject()` with no arguments). The two doors the default studio shows on Finish - the production one and Export - both save; Finish's Advanced-mode third door, "Open in the editor", is wired to the same non-saving call. `docs/backlog/create-project-is-a-door-that-saves-nothing.md` is the open question about it. | participants | the Finish step | `e2e/wizard-finish.spec.ts`; importing the same artwork twice keeps one graphic (`e2e/import-name-collision.spec.ts`, fixed 2026-09-08, and a student re-importing after a change is exactly this). | WORKS |
| R1.7 | **The take-home.** Small groups, Illustrator, two pieces each: a lower-third quiz template and a scoreboard. Started in the room, finished in their own time, sent in | participants, in groups | `/docs#svg-rules` for the rules and `/docs#svg-export` for the export settings; `docs/svg-samples/quiz-board.svg` and `scorebug.svg` are the two reference drawings, and the README beside them says what each one teaches | The two sample files are the ones `e2e/import-svg-behaviour.spec.ts` drives all the way through behaviour, so a group that follows their layer naming gets a working quiz and a working scoreboard rather than a static picture. R1.4 is what their file will do when it comes back. | **Owner call, 2026-09-10.** The product side is WORKS; what is not written yet is how a finished file is SENT IN and what happens to it. Named as a gap: §7 row 14. |

**An encore, if the room is ahead of the clock.** A live audience vote on artwork they drew,
phones at the join link (`/docs#svg-vote`). The board the owner walked on 2026-09-03 is
`e2e/fixtures/svg-corpus/illustrator-live-vote-band.svg`, a test fixture and not a shipped sample,
so it has to be brought to the room by hand. Of the three defects that walk found, row J fixed
two on 2026-09-05, stated the third rather than fixing it, and left the field-shape question open
(`docs/backlog/live-vote-fields-that-do-not-work.md`). It is not one of the two named
capabilities, so it is not a beat. Two more things stand against making it one: the shipped
`poll.svg` sample cannot be bound as a vote
(`docs/backlog/the-shipped-poll-sample-cannot-be-imported-as-a-live-vote.md`), and the two
drawing guides disagree on `Answer 1` (`docs/backlog/the-two-live-vote-guides-disagree.md`),
which is the drift §0 call 5 refuses to add to.

---

## 4. Road 2 - your coding agent

The owner drives one agent end to end on the screen. Participants with Claude Code or Codex
follow on their own machines. Everyone, subscription or not, does R2.1.

| # | beat | who | route | evidence | status |
|---|---|---|---|---|---|
| R2.1 | The CLI with no agent at all: a scaffold, validated with screenshots, saved | everyone | `npx -y @noacg/cli scaffold --type scoreboard --design neutral --out ./sb`, then `npx -y @noacg/cli validate ./sb --screenshots ./shots`, then `npx -y @noacg/cli login` once and `npx -y @noacg/cli save ./sb` | `e2e/bridge.spec.ts` ("the three scaffold paths validate clean with every field id in the markup"); `cli/test/smoke.test.mjs` walks scaffold -> validate -> inspect -> screenshot -> save against a bridge; 0.3.0 on npm since 2026-09-05, and 0.3.0 stopped loading Playwright at startup. `noacg types` against the hosted bridge measured 2.0 s cold, and the agent-round bench has driven its cells through this terminal path since 2026-08-27 (`docs/AGENT_CLI.md`, "What a session pays"). | **UNSEEN (eyes).** No person-driven run of the terminal path against `noacg.studio` is recorded anywhere, and `main` carries five `cli/` or `src/bridge/` commits since 0.3.0 shipped. §7 row 8. |
| R2.2 | With an agent: paste the one prompt | participants with an agent | `/docs#agent-install`, where the prompt installs the plugin, verifies with `noacg doctor`, and asks what to make | the page; `e2e/docs.spec.ts` pins `#claude-code` | **UNSEEN (eyes)**, see B5 for when and by whom. |
| R2.3 | Describe the graphic. The agent authors it, validates with screenshots, fixes what the validator reports | owner on screen, participants alongside | "Make me a clean scoreboard for a floorball stream: home, away, period, clock." The loop is the `noacg-graphic` skill's: author -> `validate --screenshots` -> fix -> `save` | The measured round of 2026-08-22: 25 of 25 cells validator-clean across five arms, and in the owner's blind read of 2026-08-23 every one was airable and "so much better" than the API route (`benchmarks/agent/rounds/2026-08-22/VERDICT.md`). The same day 22 of them were saved into one folder and staged as a production (`docs/SAVED_CONTENT_MODEL.md` §6). That round loaded the plugin per session; the terminal path is the bench's road since 2026-08-27, as R2.1 says. | WORKS |
| R2.4 | `noacg login` opens the consent page; `noacg save` puts it in the library and prints a link that opens at once | owner, then participants | the browser asks one question; the key can only create graphics | `docs/AGENT_SAVE.md`; `e2e/agent-access.spec.ts` (offline posture); `e2e/configured/agent-access.spec.ts` walks consent -> code -> redeem -> save 201 -> deep link -> revoke -> 401 against the real backend. No handoff records when that live spec last ran. | **UNSEEN (eyes)** since the 2026-08-23 saves; §7 row 8. |
| R2.5 | "In minutes": the clock from the prompt to the graphic on the NoaCG player | - | - | `docs/AGENT_CLI.md`, "Time to air, measured", 2026-09-09 (pull request 190): **24.8 s of tool time for the seven authoring verbs** (doctor, types, scaffold, validate, inspect, screenshot, pack), of which `validate` is the slowest at 10.7 s - all seven open a browser to reach the studio (they go through `BridgeClient.connect()`), and `validate` is the one that also runs the gate and writes three full-size frames in it; setup once per machine is 8.7 s. The same walk took the package to air through its own fallback player (Play, an operator button, Stop), each step seconds. **The cloud leg, `save` into the library and the studio's output URL, was not walked and has no time**: the CLI's key store is per origin and the dev server has no library, so `save` refused in 0.3 s. | **UNSEEN (eyes)** for the cloud leg, which is the same live `save` §7 row 8 already owes; it takes a stopwatch when it runs. Say "24.8 seconds for the seven verbs" in the room. Do not say "minutes to air" as a measured claim until row 8 closes. |
| R2.6 | The operator buttons come from the graphic's own fields and machine, no category consulted | owner | open the saved scoreboard on the production page: an input per field, ± and its events as buttons | `e2e/bridge.spec.ts` "the operator surface of a scaffold is derived from its fields and machine"; `e2e/ograf-contract.spec.ts` renders the real control components from a hand-written third-party manifest | WORKS |

---

## 5. On air - the NoaCG player on the day, their systems later

Both roads have put a graphic in the library. From here the beats are the same for both.

**On the day, the session stops at A1 and A2** - our own hosted player, seen working (§0 call 4,
owner 2026-09-10). A3 to A7 are NOT on the clock: no OBS on a student laptop, no CasparCG box in
the room, no playout configuration. They stay written here because they are true and because one
can be shown if somebody asks, and because the real playout session in the studio later is what
they are for. The hardware rows still cite the acceptance boxes the 12th will tick rather than
restating them, so a tick there is a tick here - the 12th is unchanged.

| # | beat | who | route | evidence | status |
|---|---|---|---|---|---|
| A1 | A production: the graphic in it, one cue, **Start production**. Two links appear behind **Links**: the output URL and the control URL | participants | library row -> **+ Production** -> the production page -> a cue -> **Start production** (`/docs#dashboard`) | `e2e/productions.spec.ts`, `e2e/production-persistence.spec.ts`; the 22 agent graphics staged as a production on 2026-08-23. `docs/PROMISE_AUDIT.md`, "A production ... one persistent output URL": WORKS WITH A STATED LIMITATION, the free account from B2. | WORKS |
| A2 | Take, Update, Out from the dashboard, and a phone as a second operator on the control URL | participants, phones out | the production page; the control URL on a phone | `docs/CONTROL_LAYER.md`; `docs/PROMISE_AUDIT.md`, "Drive it from another tab, an OBS dock, a phone, a second laptop": WORKS NOW, riding on the production row's account. The imported quiz over the HOSTED log: `docs/acceptance/IMPORTED_QUIZ_HOSTED_WALK.md` step 1 ran and passed 2026-08-22 (the drawn states cross the wire and boot recovery repaints them). | **UNSEEN (eyes)** for the imported quiz, because that walk's step 2 is the eyes-on half and is still owed. The 12th is it. Everything else in the row is WORKS. |
| A3 | Their system, the one everyone has. OBS on their own laptop, a Browser source loading the output URL | participants | OBS -> Sources -> + -> Browser -> paste the output URL -> 1920x1080 -> the channel's FPS (`/docs#obs`) | The route is `docs/CLOUD_PLAYOUT.md` §8 step 7 (an OBS Browser Source AND the CasparCG HTML producer, surviving a channel restart). `docs/STUDENT_RELEASE_ACCEPTANCE.md` §1 says to follow those steps but lists no line for the OBS half, so **no written tick for OBS exists anywhere**. The output page and its recovery are pinned offline; the docs page carries the four settings and the failure table. | **NOT ON THE DAY** (§0 call 4, owner 2026-09-10). Kept written for the studio playout session later and for anybody who asks in the room. The 12th is unchanged: the owner has done this and nothing records it, so on the 12th he writes the OBS line into §1 and ticks it. |
| A4 | CasparCG 2.3 with the output URL. `CG 1-20 ADD 1 "<output URL>" 1`, loaded once, cued from the dashboard | owner, on the room's box | `/docs#casparcg-url` | The same §8 step 7 as A3; its acceptance line is the one in `docs/STUDENT_RELEASE_ACCEPTANCE.md` §1 labelled §8.7 (channel restart with the URL loaded), unticked. The output page is compiled down to Chromium 75/88 with shims, which is what 2.3.x runs (`/docs#casparcg-versions`; the floor and the gate are `docs/PLAYOUT_COMPATIBILITY.md`). `docs/PROMISE_AUDIT.md`, "Point OBS, vMix or CasparCG at the output URL", grades the Connect path a limitation and says nothing about a hand-loaded URL. | **NOT ON THE DAY** (§0 call 4). The 12th still ticks the §8.7 line on the school's box, and the studio playout session later is where this beat really runs. A red result on the 12th is now one row with the box rather than a demo at risk. |
| A5 | CasparCG or SPX with no network at all. Export the package, play the file, drive it from the bundled `controlpanel.html` or the SPX rundown | owner | the graphic's Export -> **CasparCG export** or **SPX**; the CasparCG package has carried an operator page since 2026-09-04 | `e2e/exports.spec.ts` (single file survives its own autoplay, every target carries its fonts, no dangling reference in any of the six); `e2e/import-svg-behaviour.spec.ts` "CasparCG package: the standalone panel drives the imported QUIZ board through lock and reveal". On real hardware, the `docs/STUDENT_RELEASE_ACCEPTANCE.md` §2 CasparCG export box records the round-2 pass on the owner's server in its own note, and the box itself is what the 12th ticks. | WORKS |
| A6 | CasparCG Connect. Put on air from the production page through `npx @noacg/cli caspar agent` | owner | Settings -> Playout; the production page's Put on air row (`/docs#casparcg-connect`) | `e2e/caspar-connect.spec.ts` against a fake AMCP listener. Nobody has run it against real hardware, which is exactly the limitation `docs/PROMISE_AUDIT.md` states on "Point OBS, vMix or CasparCG at the output URL", and the hardware item already waiting for it is `docs/acceptance/owner-queue/2026-08-25-casparcg-connect-against-real-hardware.md`. | **NOT ON THE DAY** (§0 call 4). Do not show it. A4 does the same job by hand, in the studio session later. |
| A7 | OGraf, for Yle. Export the graphic as an OGraf v1 package (or take one of the six `/ograf` starters) and load it in an OGraf renderer | owner, or Yle on their renderer | the graphic's Export -> **OGraf**; `https://noacg.studio/ograf` | A session loaded and drove catalog and CLI-made packages in a renderer nobody here wrote, SuperFly.tv's ograf-server, on 2026-08-18 and 2026-08-22 (`docs/OGRAF.md`; the 2026-08-26 entry there is a schema check with no renderer). On **2026-09-09** the imported `quiz-board.svg` went through the same renderer WITH its behaviour - load, play, select, lock, reveal, stop and clear, driven through the server's HTTP control API, with the designer's drawn states read out of the renderer's own DOM after each press: the pick, then the LOCKED IN badge beside it, then the key lit as correct and the other three marked wrong. That round found and fixed the defect that made every operator action answer 200 and paint nothing, and isolated it by an A/B against what `main` emits (`docs/OGRAF.md`, "2026-09-09"); `scripts/ograf-external-walk.mjs` repeats the whole walk and `e2e/ograf-conformance.spec.ts` pins the fix. Every catalog manifest conforms (`docs/PROMISE_AUDIT.md`, "Any graphic made here exports as an OGraf v1 Graphic"), and an imported SVG ships through the same gate (`e2e/import-svg.spec.ts` "the export door ships the bound SVG unchanged through the gate"). Which renderer Yle runs is still not recorded anywhere. | WORKS, and off the clock (§0 call 4) unless Yle ask for it in the room. Shown on our own machine, in ograf-server. If Yle brings a different renderer it is untried there, which is what the owner-action item under B0 asks about in the same message. |
| A8 | NoaCG driving a production ON an OGraf renderer, with the renderer in control | - | - | `docs/PROMISE_AUDIT.md`, "A third-party OGraf graphic in the studio ..." and "NoaCG as the controller of a NoaCG production running on an OGraf renderer": NOT YET IMPLEMENTED | Not a beat. Said out loud if asked: today NoaCG packages and the renderer controls. |

---

## 6. Close - the guide

| # | beat | who | route | evidence | status |
|---|---|---|---|---|---|
| G1 | The step-by-step guide is `/docs`. Road 1: `#first-graphic` (the whole road, step by step), `#svg` (rules, layers, fonts, export, vote). Road 2: `#claude-code`, `#agent-install`. On air: `#dashboard`, `#obs`, `#casparcg`, `#export` | owner, pointing | `https://noacg.studio/docs` | each section pinned by `e2e/docs.spec.ts`. `#first-graphic`, "Your first graphic, step by step", walks the SVG road from a drawn file through fields and a production to air; written by walking the road, every quoted string read off a running build (pull request 191, 2026-09-09). The CLI page was end to end before it. | WORKS |
| G2 | **Two one-page indexes** (owner, 2026-09-10). The STUDENT page: the road in order, the two URLs, a link into each `/docs` anchor, and the take-home brief from R1.7 - printed for the day and shareable as a file afterwards. The OWNER page: the same beats as a running order, with who drives each and the timing from §0 | one row | two one-page PDFs, or a printed `/docs` sub-page per audience | - | **GAP**, written in the week of the 22nd, one page each on purpose (§0 call 5). The anchors they link to exist now (G1). Until they exist, the deck's notes pages print one slide per page with the notes under it. §7 row 12. |

---

## 7. What is not WORKS, and what closing it costs

This table is derived from the status column above and nothing else: every beat whose status is
GAP or UNSEEN has exactly one row here, and a beat that is WORKS has none. A beat marked NOT ON
THE DAY keeps its row, because the work is still owed to the 12th or to the studio playout session
later - the row says so in its beat cell, and its "if it stays open" column no longer describes a
risk to the 25th. It is ordered by what
would sink the day, not by size. Closing a row edits the beat's status cell and deletes the row
in the same commit, so the two never disagree; the who column is the only owner list there is.
The "what closes it" column names the mechanism outside this file that really settles the row.

| # | beat | status | what closes it | cost | who | if it stays open |
|---|---|---|---|---|---|---|
| 1 | B0 the venue network | GAP | the owner took this on himself on 2026-09-10 and the owner-action item was deleted; it closes when he has the `app?diag=1` screenshot, or decides we run on our own connection | one message | owner, personally | bring our own connection and say so. **This row got MORE important on 2026-09-10, not less**: §0 call 4 ends the day at the hosted player, so a blocked network now takes the whole session rather than one beat |
| 2 | A4 the output URL on a real CasparCG 2.3 box - **off the day, §0 call 4**; this row now serves the 12th and the studio session | UNSEEN (box) | the §8.7 line in `STUDENT_RELEASE_ACCEPTANCE.md` §1 ticked | free on the 12th; a red result is one row with the box | the 12th | A5 is the fallback and it is proven on hardware |
| 3 | A3 OBS on their own laptop - **off the day, §0 call 4**; no student configures OBS on the 25th | UNSEEN (box) | an OBS line written into `STUDENT_RELEASE_ACCEPTANCE.md` §1 and ticked | free on the 12th | the 12th | the beat runs anyway; the risk is an untried setting, not the route |
| 6 | B5 the install lines on a fresh machine, and the Codex side of the `noacg-mcp` split | GAP | both agents' install lines on a clean profile, dated in B5 | half a day, now; again after any CLI bump | one row | R2.2 fails in the room in front of the people it was for |
| 8 | R2.1, R2.4, R2.5 the terminal path and the live save, unseen since 2026-08-23, and the one leg the 2026-09-09 timing could not walk | UNSEEN (eyes) | `e2e/configured/agent-access.spec.ts` run once, plus one `save` by hand against `noacg.studio` with a stopwatch on it and on the output URL, dated in R2.5; both need the backend credentials, which live in the primary checkout's `.env` and are copied into a worktree's environment rather than run there | minutes, now | any session with the credentials | none likely; the road was person-driven 2026-08-23. "Minutes to air" stays unsaid as a measured claim |
| 9 | R2.2 the one-prompt install | UNSEEN (eyes) | closes with row 6 | - | one row | as row 6 |
| 10 | A6 CasparCG Connect on hardware - **off the day, §0 call 4** | UNSEEN (box) | the hardware item `2026-08-25-casparcg-connect-against-real-hardware.md` closed | one hour on the box | the 12th | the beat is cut; A4 covers it by hand |
| 11 | R1.4, A2 the owner's own quiz board after the fixes, and the eyes-on hosted quiz walk | UNSEEN (eyes) | his walk; `IMPORTED_QUIZ_HOSTED_WALK.md` step 2 marked run | ten minutes each | owner, the 12th | the road is pinned three ways; the risk is taste, not function |
| 12 | G2 the printed one-page index | GAP | one page from this file and the deck's anchors | one afternoon, last | one row, week of the 22nd | the room is handed `/docs`, and the deck's notes pages print one slide per page |
| 14 | R1.7 how a finished take-home file comes BACK | GAP | a written answer to where a group sends its quiz template and scoreboard, in what format, and what we do with it - one paragraph in the student one-pager (G2) is enough | one row, with G2 | one row, week of the 22nd | groups finish the artwork and there is nowhere to send it, which wastes the only piece of real work the session sets |
| 13 | O2 the deck, which now CONTRADICTS §0 and must be rebuilt before it is walked | GAP | rebuild slides 5, 3 and 7 against calls 4, 5 and 6, then his walk of `docs/presentation-2026-09-25/NoaCG-2026-09-25.pptx`; any edit he makes wins over the generator | one row, then ten minutes of his | one row, then owner | **he opens it and it tells the room to point OBS at the output URL**, which the 2026-09-10 call removed. Slide 5 shows four playout targets, slide 3 does not mention the small-group take-home, slide 7 does not mention the two printed pages. `docs/backlog/deck-contradicts-the-2026-09-10-calls.md` |

Rows 4 and 5 were closed on 2026-09-09 by pull requests 190 and 191, and row 7 the same day by
the ograf-server walk in A7; the numbers are not reused, so a reference to a row in a slide note
or a handoff stays true.

Not a gap, but a decision to record: nothing here needs money, an account we do not hold, or
anything published past `main`. The release workflow the owner already ruled a session may run
(`docs/AGENT_CLI.md`, "Releasing to npm") publishes any CLI version bump.

---

## 8. Keeping this true

One rule: closing a §7 row edits the beat's status cell and deletes the row, in the same commit,
and the row's number is never reused. Whoever runs the 12th owns rows 2, 3, 10 and 11; the owner
owns row 13 and, through the walk item, any wording on the deck. The status column is hand-maintained and the "what closes
it" column names the file that really moves, so a reader who doubts a cell checks that file. A beat
whose status is wrong on the 24th is worse than a beat that is missing.
