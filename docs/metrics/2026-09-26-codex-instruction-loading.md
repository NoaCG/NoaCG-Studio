# Codex instruction loading, 2026-09-26

## Spec

- Goal: preserve native Codex startup guidance and retrieve only missing folder guidance before editing, with transcript evidence from root and nested starts.
- Non-goals: change Claude loading, global instructions, Claude memory, product code, or introduce a new instruction framework.
- Decisions: extend the existing lookup with repeatable `--loaded <AGENTS.md>` inputs for files already in context; never infer startup context from a shell's current directory. Keep unflagged lookup complete. Ratchet the native byte cap using measured chain sizes.
- Verify: regression tests for mixed generated/hand-written contracts and missing guidance, fresh read-only Codex probes before/after, largest-chain startup, shared-instruction/contracts gates, full build and the check/queue workflows.

## Baseline

- `gh pr view 423 --json state,mergedAt,url,mergeCommit`: MERGED at 2026-09-26T06:04:41Z, merge d91f3bb4f20bc3975ea4a8174d70751c3fea9136. `git fetch origin`; this clean worktree branched from origin/main as `codex/instruction-loading-audit`. No work or build in the primary checkout.
- `codex --version`: 0.158.0-alpha.13. Probes use the configured model/account, `--sandbox read-only --json`, and no edits/build/commit/delegation. Prompt: imagine a comment edit to `src/templates/tickers/tk01.ts` and `e2e/control.spec.ts`; first inventory startup context without tools, retrieve normal pre-edit guidance, read five lines per target, report commands and duplicates. Nested prompt uses paths relative to tickers.
- Root: `codex exec -C <worktree> --sandbox read-only --json <prompt>`, session `01a0dc65-edf1-7fe3-a897-06b142be820a`. Global and root at startup; skill catalog only, no bodies. Ran `npm run rules -- src/templates/tickers/tk01.ts e2e/control.spec.ts`. Output truncation caused three overlapping partial re-queries. This is not an exactly-once delivery guarantee.
- Nested: same command with `-C <worktree>/src/templates/tickers`, session `01a0dc66-542e-77f2-aa13-be27ddc89105`. Saved user instruction message is 46,870 characters; global, root, generated `src` and `src/templates`, and hand-written ticker contract present once. The agent changed shell cwd to root for the lookup: current shell directory cannot identify native startup loads. Lookup repeated the ticker body and generated rules. New e2e guidance was partly truncated and not recovered.
- `node scripts/check-shared-instructions.mjs`: 55 chains; largest 65,716 bytes, cap 110,000. Global file separately 2,355 bytes. Config has no fallback filenames or custom instruction override; no global AGENTS.override.md. Root on disk is 2,983 bytes against compiler budget 3,072.
- `npm run audit:instructions`: 496 active rules, 483 prose-only, zero exact duplicate sentences, 18 prose rules naming possible carriers, two Claude-only global skill names (`condense-doc`, `synced`). Those are inventory differences, not evidence that Codex needs the same skills.

## Remeasurement and changes

- `npm run rules -- src/templates/tickers/tk01.ts e2e/control.spec.ts --loaded AGENTS.md --loaded src/AGENTS.md --loaded src/templates/AGENTS.md --loaded src/templates/tickers/AGENTS.md`: nested session `01a0dc6b-bb02-7091-bbe3-bf82379c59c7` received only the e2e body and `e2e/wait-preview-rebuild-what-document-shows`, without native duplicates or truncation (22,246 output characters). It first mistyped the e2e path, then corrected it; the first lookup emitted no guidance. Explicit loaded-file inputs survive its change of shell cwd. They are declarations by the caller, not automatic session tracking.
- The lookup previously emitted three proven carried template rules, despite their absence from compiled contracts. `parseRules` does not establish `carried`; `validateAgainstTree` does. The lookup now reuses that proof for its selected rules. `--area` stays a full reference listing, including carried rules. A missing/unproven carrier still leaves the sentence visible.
- Root re-probes `01a0dc6b-e9db-7cb0-9f54-05415a5715a7` and `01a0dc6e-5028-7933-a510-1f3eef89829f` used `npm run rules -- src/templates/tickers/tk01.ts e2e/control.spec.ts --loaded AGENTS.md`: both returned complete source output (39,823 characters, 43 scoped rules), but the outer tool wrapper truncated 84/86 tokens. Each recovered a paragraph using `Select-String -LiteralPath e2e/AGENTS.md -Pattern 'A spec in' -Context 0,5`, with overlap. Increasing shell output alone did not solve wrapper truncation. Trial root wording about sizing both budgets did not help and was removed. **Exactly-once delivery is not established for large multi-folder output.** Narrower e2e guidance is a recommendation, not a silent rewrite of Claude's contract.
- `codex exec -C <worktree>/src/components/wizard --sandbox read-only --json <startup-inventory-only-prompt>`: session `01a0dc6c-8452-73d2-9b81-9ca2c15203dc` contained the global, root, src, components and wizard bodies, 173 unique native rule IDs and the final `wizard/write-speed-buttons-follow-template-own` sentence. No tools or skill bodies loaded. This proves the actual configured limit admits more than the default 32 KiB and the complete largest chain. After final preamble shortening, `node scripts/check-shared-instructions.mjs` reports 65,730 bytes and 7,998 bytes free under 73,728; the measured earlier preamble was larger.
- Session rollouts inspected under `~/.codex/sessions/2026/09/26/`: startup user messages contain one global copy and no repeated compiled IDs (root: 10; tickers: 123). Repo skills appear as catalog metadata, not procedure bodies. The explicit `$start` probe, `codex exec -C <worktree> --sandbox read-only --json '$start'`, session `01a0dc6e-c10f-7d72-a0de-8f07827cf3ec`, loaded its skill body on invocation, read `.agent-workflows/start.md`, then needlessly fetched `AGENTS.md`. The Codex-only adapter now satisfies that read from complete native context, retaining the fallback read when absent. Claude's adapter and shared workflow are unchanged.
- Read user/repo config and enumerated override files: user model is `gpt-6-astra`, effort `medium`; the primary repo is trusted; repo config sets the size limit. No configured fallback filenames, no custom instruction-file setting, no repo/global override file. No `CLAUDE.md` fallback was introduced. Official native discovery reference: https://learn.chatgpt.com/docs/agent-configuration/agents-md. The observed native chains agree with it; arbitrary fallback/override combinations were not exercised.
- Read `scripts/instruction-audit.mjs` and the global skill directories: `synced` is a container with UUID subdirectories, not a top-level skill. The audit compares directory names, so that warning is not evidence of a missing skill. `condense-doc` is an actual Claude-only skill. Neither warrants cloning all Claude skills. The probes' plugin catalog sizes varied, but the relevant repository workflow entries remained present; all installed plugin catalogs were not audited.
- Global file and all 22 Claude memory markdown files were read, never edited (`Get-ChildItem .../memory/*.md | Measure-Object Length -Sum`: 30,042 bytes). Memory is not in these Codex startup transcripts. No Claude runtime probe was repeated: `git diff` shows no Claude adapter, rule surface, wrapper, or memory changes. Native repo config and Codex start adapter are the only tool-specific settings changed; root generation changes only Codex retrieval wording.
- Repeating `$start` after the adapter change, session `01a0dc70-ad72-7091-8c98-e353c84ed4d3`, read only `.agent-workflows/start.md` and replied Ready: zero root rereads. Counting `.agents/skills` catalog entries in the nested rollout found each of the 14 implicit skills once; `cleanup-worktrees` is absent from the model catalog and its `agents/openai.yaml` sets `allow_implicit_invocation: false`. That absence is intentional; its destructive workflow was not invoked.

## Check evidence

`node --test scripts/rules.test.mjs`: 4 passed. `node --test scripts/compile-contracts.test.mjs`: 28 passed. `npm run check:contracts`: 525 rules, 272 generated files current. `node scripts/check-shared-instructions.mjs`: passed, including the changed Codex-only adapter. `git diff --check`: clean.

The check workflow handed the exact seven-file scope and merge base d91f3bb4f20bc3975ea4a8174d70751c3fea9136 to `codex exec --sandbox read-only review - --json` through stdin. Its returned clean review names the same base and all seven files; scope checked against the working-tree diff plus the untracked report. Review: delegated, no actionable findings. Simplify: inline (no dedicated simplifier available), no code changes needed after inspecting helpers, filtering, argument handling and adapter boundaries. The report's memory-byte typo was corrected against the actual file sum. Full build and landing status are recorded by the check stamp and pull request, not inferred from the focused tests. Taste: not applicable; no graphic or product code changed.

## Owner decisions: proposed only, in priority order

1. **Move current product decisions out of exclusive memory.** Keep memory as a pointer to repo decisions with date/supersession. `Get-Content .../memory/*.md`, `rg` over `docs/OWNER_RULINGS.md`, and the root contract show a concrete conflict: `product-direction.md` calls the editor Advanced mode while `root/render-old-code-editor-route-never` says that route and stored preference are gone. Reconcile the current direction before trimming words.
2. **Merge the two taste memories.** `owner-taste-rules-composition.md` still states rule 5 as binding; `owner-taste-rules-instrument.md` says it was withdrawn and only reports. Keep the owner's quotations and ratified floors together with their exceptions, then link the shared repo decision. Do not turn the 28 px eligibility floor into a universal design gate.
3. **Move operational state to its owner.** Money targets/approval boundaries remain useful memory; measured generation prices belong in dated metrics, the open firewall action in an owner-action receipt, and business-dashboard architecture in that separate repo. Cut shipped status (`pictures ... shipped`, `Only credits is done`) from memory after checking current code. No current prices, firewall state, dashboard repo or full catalog status were verified here.
4. **Keep the global decision boundary; merge generic wording.** Dictation, owner-only choices, unattended behavior, simple design, evidence, and irreversible-action approval earn their space. Merge the two communication bullets; cut generic “find facts yourself” and duplicate verification wording if room is needed. A useful missing cross-agent preference is the owner's Helsinki timezone (currently only in Claude memory); propose replacing low-value prose rather than growing past 2.5 KB. Do not add repo-specific machinery globally.
5. **Cut the root clean-code platitude and stale test claim.** `npm run rules -- --area root` shows `emit-clean-commented-code-person-can`, already covered by global maintainability guidance, and “There are no unit tests” in `run-after-changes-keep-tree-lint`; this change alone runs 32 Node tests. Keep required build/browser evidence but make the wording true. Keep canonical code, export validation, client independence, landing ownership and human acceptance as project invariants.
6. **Move enforceable prose to proven carriers, selectively.** The audit lists 18 candidates, not 18 safe deletions. Start with `model/imports-nothing-touches-dom-storage-deliberate` (import boundary), `templates/escape-field-text-shared-data-reading` (escaping), and `templates/name-every-layer-svg-you-draw` (layer names). Confirm failure messages and relevant trigger coverage before retiring prose; source references alone do not prove a check reaches Codex. Claude-only hooks are not Codex enforcement.
7. **Split the e2e contract by task, without creating another delivery framework.** Root lookup output was truncated in three measured sessions. Its hand-written file mixes writing a spec, cloud execution, browser lifecycle, paid benches and stale incidents. Keep local spec-writing hazards there; move runner/bench procedure to the relevant existing workflow or helper. The native ticker startup also brings 123 rules for a two-file task: prefer root starts by default rather than deleting useful nested discovery.
8. **Shorten shared workflows and stop requiring rereads.** `Get-Content .agent-workflows/check.md | Measure-Object -Line -Character` reports 17,550 characters; much of review/queue guidance repeats incident histories and Claude notification routing. Keep executable steps, scope check, modes and failure handling; move history to records, and qualify Claude-specific claims. The check workflow's direct nested-contract reread conflicts with the root's single lookup. The Codex start adapter is fixed here; shared workflow changes need owner scope approval.
9. **Keep monthly audit; add event-driven measurement.** Run a root/nested/explicit-skill probe after changing instruction delivery or upgrading the harness, and record commands, version, native IDs, tool reads and truncation. Monthly counts miss the measured native-plus-lookup duplication and wrongly treat directory parity as skill parity. Before adding a lesson, require the concrete failure, cause fix/check considered, narrow trigger, and retirement condition. Owner prompts should give outcome and constraints; ask for exceptions, not repeated confirmation of normal workflow.

Raw rollouts remain in the local Codex session store; they are not committed because they include unrelated harness context. This report does not claim a desktop fresh-start test, compaction/resume coverage, every skill invocation, or live Claude hook delivery. No product/browser behavior changed, so browser/e2e execution and graphic taste review are not applicable.
