# components/mark-every-asset-row-usage-tick

Rule: `components/mark-every-asset-row-usage-tick`. Recorded 2026-09-07 on `claude/components-contract-migration` at 550fd5cf.

Name, format, dimensions, aspect, size, alpha, Lottie timing and video duration are all derivable from the bytes, so holding them in the model would be a second source of truth that goes stale on the first edit.
