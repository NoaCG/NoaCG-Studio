# components/keep-panel-productions-block-slim-create

Rule: `components/keep-panel-productions-block-slim-create`. Recorded 2026-09-07 on `claude/components-contract-migration` at 550fd5cf.

Student release step 8 in docs/GOALS_ARCHIVE.md put the block here so a graphic can join a show without leaving the editor, not so the dashboard could be rebuilt in a panel.
