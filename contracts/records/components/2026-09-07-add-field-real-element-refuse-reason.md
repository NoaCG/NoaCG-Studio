# components/add-field-real-element-refuse-reason

Rule: `components/add-field-real-element-refuse-reason`. Recorded 2026-09-07 on `claude/components-contract-migration` at 550fd5cf.

The old fallback appended a definition-only field no element answered, which is the exact defect class scripts/field-coverage.mjs exists to catch. The contract this replaced still said long text, off-design images and off-shape templates keep that fallback; they are refused with an explanation instead.
