# components/build-sample-data-graphic-open-project

Rule: `components/build-sample-data-graphic-open-project`. Recorded 2026-09-07 on `claude/components-contract-migration` at 550fd5cf.

The window takes the values as part of its request rather than reading the store, so the caller owes the sync. A slot that was empty stays empty.
