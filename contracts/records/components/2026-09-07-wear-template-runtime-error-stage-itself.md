# components/wear-template-runtime-error-stage-itself

Rule: `components/wear-template-runtime-error-stage-itself`. Recorded 2026-09-07 on `claude/components-contract-migration` at 550fd5cf.

Before the badge, a template whose JS threw at load was a silently broken canvas unless the Export panel happened to be open. Pinned by e2e/preview-error.spec.ts.
