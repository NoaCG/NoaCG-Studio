# components/keep-hosted-analytics-explicit-opt-consent

Rule: `components/keep-hosted-analytics-explicit-opt-consent`. Recorded 2026-09-07 on `claude/components-contract-migration` at 550fd5cf.

The banner only renders when a backend is configured, the browser is not blocking analytics, and the reader has not decided; everything else about it is the backend's own contract.
