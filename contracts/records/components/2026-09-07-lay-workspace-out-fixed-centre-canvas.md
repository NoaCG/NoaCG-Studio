# components/lay-workspace-out-fixed-centre-canvas

Rule: `components/lay-workspace-out-fixed-centre-canvas`. Recorded 2026-09-07 on `claude/components-contract-migration` at 550fd5cf.

The dock model replaced a fixed three-column editor. A tab moves to another dock or closes from its own menu and a dock's plus button re-adds a hidden panel; a dock that rendered while empty left a dead splitter nobody could get rid of.
