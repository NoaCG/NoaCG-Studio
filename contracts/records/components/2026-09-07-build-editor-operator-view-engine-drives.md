# components/build-editor-operator-view-engine-drives

Rule: `components/build-editor-operator-view-engine-drives`. Recorded 2026-09-07 on `claude/components-contract-migration` at 550fd5cf.

The same engine emits the standalone controlpanel.html download, so a legality rule written in the panel rather than the engine would grey differently in the app and on air.
