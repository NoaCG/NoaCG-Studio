# components/treat-reveal-signal-rather-than-state

Rule: `components/treat-reveal-signal-rather-than-state`. Recorded 2026-09-07 on `claude/components-contract-migration` at 550fd5cf.

The wizard reveals Export after an import and Data after an Import Graphic create, and both can be a request for the panel that is already stored. A value-keyed effect also fired on mount and on a StrictMode remount, overriding the tab the reader had left open.
