# components/open-inspector-tool-panels-right-dock

Rule: `components/open-inspector-tool-panels-right-dock`. Recorded 2026-09-07 on `claude/components-contract-migration` at 550fd5cf.

The default changed without a version bump on purpose: anyone with a saved layout keeps their docks exactly as they left them. The contract this replaced still described the older default, code docked left, which was the wrong thing to copy when building a new shell.
