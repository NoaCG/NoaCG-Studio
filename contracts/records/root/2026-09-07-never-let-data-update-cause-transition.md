# root/never-let-data-update-cause-transition

Rule: `root/never-let-data-update-cause-transition`. Recorded 2026-09-07 on `claude/root-contract-migration` at 39835021.

The default path is the SPX and CasparCG compatibility contract: every template, however complex, degrades to dumb-stepping along it. A payload may ride an accepted event, which is what makes a multi-part change atomic.
