# root/give-input-only-holder-never-inline

Rule: `root/give-input-only-holder-never-inline`. Recorded 2026-09-07 on `claude/root-contract-migration` at 39835021.

The editor entrance reset clears inline properties across the whole root subtree, so an inline-hidden holder comes back VISIBLE on the canvas and the raw value airs. A stylesheet rule cannot be cleared that way.
