# root/enqueue-browser-driving-work-rather-than

Rule: `root/enqueue-browser-driving-work-rather-than`. Recorded 2026-09-07 on `claude/root-contract-migration` at 39835021.

Waiting in the foreground used to lose hours: the shell tool is killed at 600 seconds with the wait still running. A job known to the guard hook but not the process detector is neither blocked nor detected. The `:queued` scripts remain for when a gate needs the verdict now, because a gate cannot take a job id for an answer.
