# root/make-every-state-enterable-two-ways

Rule: `root/make-every-state-enterable-two-ways`. Recorded 2026-09-07 on `claude/root-contract-migration` at 39835021.

The queue living in the template is what makes the determinism hold identically in the editor, in an exported overlay and under SPX. Control pages are GENERATED from the machine, so legality is the structural guard mirrored as greying.


Scope corrected the same day. It named `src/components/control/**`, which has no files: the control surface has not been split out yet, and the rule was written against where the code is GOING rather than where it is. A scope that matches nothing means a rule that never loads while the store still lists it, so the compiler now refuses one.
