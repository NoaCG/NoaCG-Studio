# blocks/canonical-fixed-key-order-fixed-space

Rule: `blocks/canonical-fixed-key-order-fixed-space`. Recorded 2026-09-07 on `claude/migrate-agents-contract-rules-5ab9fc` at 39835021.

Without the fixed point every edit rewrites the whole literal, which turns each keyframe drag into an unreadable diff and makes the code view useless as the source of truth.
