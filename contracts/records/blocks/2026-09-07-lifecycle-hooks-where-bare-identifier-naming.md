# blocks/lifecycle-hooks-where-bare-identifier-naming

Rule: `blocks/lifecycle-hooks-where-bare-identifier-naming`. Recorded 2026-09-07 on `claude/migrate-agents-contract-rules-5ab9fc` at 39835021.

docs/TIMELINE_V2_PLAN.md 3b. The data must never carry executable code: the literal is parsed as strict JSON and read by the runtime in every export, so an eval there would be an injection point in a broadcast graphic.
