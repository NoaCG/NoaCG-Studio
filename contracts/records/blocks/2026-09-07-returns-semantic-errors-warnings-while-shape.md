# blocks/returns-semantic-errors-warnings-while-shape

Rule: `blocks/returns-semantic-errors-warnings-while-shape`. Recorded 2026-09-07 on `claude/migrate-agents-contract-rules-5ab9fc` at 39835021.

The rule is written once, so a finding cannot appear on the graph and not in the export report or the other way round. Split out because a branch state can carry a whole hand-built timeline and still never be entered, and leaving that to the Export panel meant the surface that let you build it said nothing.
