# blocks/legacy-region-importer-cannot-read-never

Rule: `blocks/legacy-region-importer-cannot-read-never`. Recorded 2026-09-07 on `claude/migrate-agents-contract-rules-5ab9fc` at 39835021.

Ratified in docs/DYNAMIC_MOTION_SCOPE.md 8.1. Regenerating it would discard the owner's tuning, which code is the single source of truth forbids. A legacy region still exists in SAVED templates: a project made before the migration, an import, hand-written GSAP. A region the importer CAN read gets the step timeline read-only with use-keyframes one click away, which shows the real choreography converted rather than a lesser second editor.
