# blocks/gate-code-never-category-standard-contract

Rule: `blocks/gate-code-never-category-standard-contract`. Recorded 2026-09-07 on `claude/migrate-agents-contract-rules-5ab9fc` at 39835021.

Fixed contracts keep the mask wrapper but speak a different vocabulary - a scoreboard's cells are {p}-team/{p}-score, a quiz's rows are their own shape - and data-driven categories keep hidden textarea sources. Without the host filter an insertion silently downgraded every later add-field.
