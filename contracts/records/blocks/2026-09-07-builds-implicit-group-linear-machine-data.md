# blocks/builds-implicit-group-linear-machine-data

Rule: `blocks/builds-implicit-group-linear-machine-data`. Recorded 2026-09-07 on `claude/migrate-agents-contract-rules-5ab9fc` at 39835021.

Deriving rather than persisting is why the whole catalog gained dispatch, snap and introspection without one template file changing. V1 parity holds because next() fires OPERATOR arrows only - the stop edge never makes a press legal. `parseAnimData` injects the same lifecycle pair into explicit machines where the seat is vacant.
