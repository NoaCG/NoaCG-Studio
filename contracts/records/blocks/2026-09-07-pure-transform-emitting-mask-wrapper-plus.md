# blocks/pure-transform-emitting-mask-wrapper-plus

Rule: `blocks/pure-transform-emitting-mask-wrapper-plus`. Recorded 2026-09-07 on `claude/migrate-agents-contract-rules-5ab9fc` at 39835021.

Optional `align` writes the anchor and sizes the default slot from it outwards, and optional `lineHeight` pins the span's line-height; both exist for the Prepare step's seeded field, built from the erased text's MEASURED ink (assets/eraseRegion `RegionInk`) so the line lands where that text was.
