# blocks/way-emit-preset-region-template-structure

Rule: `blocks/way-emit-preset-region-template-structure`. Recorded 2026-09-07 on `claude/migrate-agents-contract-rules-5ab9fc` at 39835021.

No preset's taste is written down twice - emit here, convert there - so the way OUT of legacy code leads to data rather than to another legacy region. The AI caller matters because the worked example in the system prompt is the motion the model is asked to ship, so a change here changes what every generated graphic imitates.
