# blocks/reads-lit-card-back-data-times

Rule: `blocks/reads-lit-card-back-data-times`. Recorded 2026-09-07 on `claude/migrate-agents-contract-rules-5ab9fc` at 39835021.

`universalPick` is applied at build through the same `applyMotionPreset`, so the wizard preview, the created graphic and the page that reads it back agree by construction.
