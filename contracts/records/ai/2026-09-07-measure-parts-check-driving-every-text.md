# ai/measure-parts-check-driving-every-text

Rule: `ai/measure-parts-check-driving-every-text`. Recorded 2026-09-07 on `claude/ai-contract-migration` at 39835021.

Reading the markup cannot see a standings row, a ticker item or a credits line that a runtime BUILDS from one field, so a design that carries them looks empty. Opacity is ignored on purpose: a region the machine reveals in a later step is transparent during the entrance and perfectly reachable.
