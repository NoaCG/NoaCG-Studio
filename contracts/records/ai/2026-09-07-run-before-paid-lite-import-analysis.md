# ai/run-before-paid-lite-import-analysis

Rule: `ai/run-before-paid-lite-import-analysis`. Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

Migrated from src/ai/AGENTS.md, paragraph 69 (zero-based blank-line inventory). Checked against code: ai-bench-preflight.mjs accepts only lite and import-analysis tasks and invokes runTaskPreflight; aiBenchPreflight calls liteProfile under a restored environment and reports overridden, unapproved, unconfigured and non-distinct arms. Source prose (historical evidence; only the rule above is authoritative): **Run `npm run bench:preflight -- <models>` before any paid round.** It is free, reaches no network, and answers what the paid runner structurally cannot: given this `.env` and these candidates, what would each arm ACTUALLY serve? `api/_lib/aiBenchPreflight.ts` resolves every arm through the REAL `liteProfile` + task registry (never a model of their rules - a preflight that reimplements the server drifts from it and then certifies runs the server will refuse), and refuses a plan whose arms are overridden, unapproved, unconfigured, or not pairwise distinct. Each of those wasted a real round: they are invisible in the OUTPUT, because a comparison whose arms resolve to one model still produces differences - sampling noise reads as model character.
