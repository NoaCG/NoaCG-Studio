# ai/keep-wizard-route-pro-generation-composed

Rule: `ai/keep-wizard-route-pro-generation-composed`. Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

Migrated from src/ai/AGENTS.md, paragraph 73 (zero-based blank-line inventory). Checked against code: AiStep calls generateProGraphic once and composes package members; pipeline.ts invokes generateDesignLanguage, composeGraphic and validateProLanguage. Source prose (historical evidence; only the rule above is authoritative): **LIVE since 2026-08-15 (hosted deployments where `AI_PRO_ENABLED` is on).** Pressing Create on the Pro tier runs **ONE text call for a design LANGUAGE**, and the platform composes **every graphic the user asked for** in it. The tier, its Phase A composer (`pro/language/`), its server booking and the findings the deleted concept-and-reconstruct engine paid for moved to **`src/ai/pro/AGENTS.md`** (with its thin `CLAUDE.md`), which loads when you work in that directory. Two things about it bind from out here: `pro/language/pipeline.ts` is the ONE route from the wizard to a Pro graphic and `pro/language/gate.ts` the ONE seam a composed graphic is scored through, and `pro/types.ts` is the wire vocabulary both TypeScript trees compile against - dependency-light for the same reason `lite/types.ts` is.
