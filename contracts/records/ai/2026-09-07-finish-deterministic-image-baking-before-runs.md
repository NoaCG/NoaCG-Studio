# ai/finish-deterministic-image-baking-before-runs

Rule: `ai/finish-deterministic-image-baking-before-runs`. Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

Migrated from src/ai/AGENTS.md, paragraph 60 (zero-based blank-line inventory). Checked against code: targetsOf matches img src before collecting classes/ids; productionSpxValidator merges asset integrity after the base validator; specToTemplate performs fixed asset baking before returning. Source prose (historical evidence; only the rule above is authoritative): **The as-is screen (`assetIntegrity.ts`)** is the protection "use it as it is" promises: a design putting a filter, crop, mask, `object-fit: cover`, rounded corners or an uneven scale on a protected picture is REJECTED. It reports through the injected validator (composed in `productionSpxValidator`, beside the safety screen), so a violation reaches the repair loop rather than only the result card. Same honest limit as `safety.ts`: it reads CSS text, not the resolved cascade. **It finds a protected picture by its `<img src>`, so anything that bakes a `src` must run BEFORE it** - a gate that runs before the last deterministic step measures a document nobody will ever see.
