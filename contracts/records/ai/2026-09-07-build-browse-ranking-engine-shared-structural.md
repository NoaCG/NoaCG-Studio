# ai/build-browse-ranking-engine-shared-structural

Rule: `ai/build-browse-ranking-engine-shared-structural`. Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

Migrated from src/ai/AGENTS.md, paragraph 23 (zero-based blank-line inventory). Checked against code: briefTerms extracts terms; shortlistFor sums browseTemplates scores weighted by log(pool.length/hits.length), applies RELEVANCE_FLOOR and orders selective hits, untouched designs, then residue. Source prose (historical evidence; only the rule above is authoritative): `shortlistFor(brief, intent, options?)` narrows it with **no new model call and no second retrieval system**: the ranking is the Browse storefront's engine (`templates/search.ts`), the structural filter is the ONE anchor table, and both read what the intent stage already produced. Four measured properties make it usable rather than merely shorter - a brief is a SET of terms (token-AND); each term is weighted by how RARE it is in the pool; the cut is RELATIVE to the best match (a nonzero score is not relevance); and the floor of four is filled in BANDS, selectively-named designs first, the residue last. `Shortlist.reason` states the split, so a shortlist never reads as four answers when two are floor-filling. The measurements are `docs/ADAPT_FIRST_PLAN.md` §3.1.
