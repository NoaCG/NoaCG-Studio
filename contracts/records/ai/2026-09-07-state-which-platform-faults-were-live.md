# ai/state-which-platform-faults-were-live

Rule: `ai/state-which-platform-faults-were-live`. Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

Migrated from src/ai/AGENTS.md, paragraph 90 (zero-based blank-line inventory). Checked against code: The source records structured-output decoding and creative root-collapse incidents that invalidated paid comparisons and a human review. This migration checks claims against the current tree, not those old scores. Source prose (historical evidence; only the rule above is authoritative): **The second one retro-invalidated a human verdict.** The owner's blind review that evening judged frames of which half carried the bug, and its "content on the frame edge" note was the bug, not taste. So: **any conclusion drawn from a round must state which platform faults were live during it**, and a round whose harness changed underneath it is not comparable to one that ran before.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

Migration report for src/ai/AGENTS.md (2026-09-07). Wrote 64 active rules and 64 records under contracts/rules/ai and contracts/records/ai using npm run learn only, plus its generated surfaces. No source code, AGENTS.md or CLAUDE.md was hand-edited; no commit, push or queue. The requested backlog was absent from this checkout and was read with git show a330afff:docs/backlog/are-the-big-contracts-still-worth-loading.md. Classification is by meaningful paragraph/list item; a mixed paragraph can retain a verified clause while its false claim or historical account is reported below. There are 14 false-claim entries and 19 true-but-probably-dead entries. Historical model calls, measurements and prices remain dated receipts, not newly verified present-day facts. --distinct count: 0; pairs: none. No learned rule was folded into a pre-existing wizard/templates rule; the repeated first-rule call only appended the complete evidence after Windows truncated a multiline CLI argument. Source quotations below preserve original wording with line breaks flattened. Code quotations use ellipses for omitted spans. The final gate results will be appended after both checks.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F1: Adapt-first is not the universal live strategy.** Source paragraph 6: "**The strategy that won is ADAPT-FIRST** (`docs/ADAPT_FIRST_PLAN.md`): the model retrieves a proven catalog design and adapts it - brand colours, a logo, the user's words, typography and proportions inside a clamped range. **It does not invent a layout.** Five paid rounds measured cheap models composing a broadcast graphic from a blank stylesheet and none produced one the owner would air; the catalog is both the crutch and the moat. Retrieval is that doctrine in code, so it is the section directly below." Code (src/ai/claudeProvider.ts:1290): "const exampleVariant = spec ? variantsFor(spec.category)[0] : undefined; ... const change = await generateValidated(content, context, undefined, options, run, exampleVariant);" Assessment: The general provider still routes custom briefs to a free-form coder. The paragraph's area-wide 'It does not invent a layout' is false; the binding catalog constraint is retained only on grounded/Lite scopes.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F2: Catalog-only generation is not enforced on every route.** Source paragraph 7: "**A generated graphic can only be a category the catalog already carries.** Lite's allowlist is a subset of the catalog's categories (today: lower thirds), and no path widens it at generation time. That constraint is not a limitation to route around: it is what guarantees a generated graphic is **operable in the control panel through the same machine, fields and events as a hand-picked one**, because it was assembled by the same `variant.create()` the wizard runs. A graphic outside those categories would carry no machine the control layer knows how to drive." Code (src/ai/claudeProvider.ts:1290): "const forceCustom = route?.route === 'create'; ... const change = await generateValidated(content, context, undefined, options, run, exampleVariant);" Assessment: CREATE explicitly produces unrestricted code instead of variant.create output; only the Lite/grounded restriction survives as scoped rules. A GraphicType is not the only way the control layer can read a machine.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F3: Three tiers are reachable when hosted Pro is available.** Source paragraph 13: "**LIVE.** One door, and behind it three tiers - two of which a user can reach today." Code (src/components/wizard/steps/AiStep.tsx:376): "const proOffered = proHosted && isBackendConfigured(); ... const tierOptions = TIER_OPTIONS.filter((option) => option.id !== 'pro' || proOffered);" Assessment: TIER_OPTIONS contains lite, pro and custom; the conjunction offers all three. 'Two ... today' is an obsolete deployment claim.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F4: Discovery permits missing prices and normalizes more than the prose says.** Source paragraph 18: "**Every model row carries a price per 1M tokens and says which key pays it.** None of the three direct provider APIs publishes a price with its listing (measured), so `aiModelDiscovery.ts` reads the managed catalog as a PRICE BOOK, matched by `modelPriceKey` - which normalizes away the vendor prefix, the dot-vs-dash separator and the dated snapshot suffix, and nothing else. An ambiguous or unmatched model is left unpriced and unsuggested rather than guessed at; the model box takes free text, so nothing is ever blocked by the book being wrong." Code (api/_lib/aiModelDiscovery.ts:169): "return withoutVendor.toLowerCase().replace(/-latest$/, '').replace(/-\d{4}-\d{2}-\d{2}$/, '').replace(/-\d{8}$/, '').replace(/[^a-z0-9]/g, ''); ... if (facts.size === 0) return models;" Assessment: modelPriceKey also lowercases, removes -latest and strips all non-alphanumerics. pricedDirectModels keeps unpriced suggestions when the book is empty; AiDiscoveredModel prices and paidBy are nullable/optional. The absolute price and no-suggestion claims were not migrated.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F5: The design call is not the only mandatory stage on default routing.** Source paragraph 31: "1. **Design spec** (`designSpec.ts`, forced `emit_design_spec`) - the only mandatory model call and the    ROUTER. Returns `fit: 'catalog' | 'custom'` plus every design parameter: chassis (`variantId`),    lines, palette/font/zone/size, animation preset, real COMPOSITIONAL parameters (typography scale    ratio/weight/tracking, density, alignment, shape/panel), `referenceSystem`, and an optional    `flourish`." Code (src/ai/claudeProvider.ts:1246): "const mode: GenerationMode = options?.mode ?? 'auto'; const { intent, route, shortlist } = await intentAndRoute(prompt, userContent, mode, run, options, context);" Assessment: Auto/create attempt a forced intent call before the design call; only explicit adapt skips it. The design tool fields remain real, but this universal call-count statement is not a rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F6: The custom coder example is the first category variant, not a nearest-design search.** Source paragraph 31: "5. **Custom path** - briefs whose STRUCTURE no catalog family carries go to the free-form coder: house    contracts + the NEAREST catalog variant's real `create()` output as the canonical example + the    design stage's direction, then the validated repair loop (`shared/repairLoop.ts` - THE one bounded    errors-back loop both the SPX and video coders drive: `MAX_REPAIR_ROUNDS = 2`, RE-VALIDATED every    round; what counts as BLOCKING stays each caller's policy, injected as a filter).    **The region contract is authored, not emitted:** the example's ANIMATION region is shown in its    AUTHORING shape (`emitPresetRegion`) and the prompt teaches that grammar - natural GSAP the model is    reliably good at, instead of the strict-JSON data block it reliably got wrong. Every emit runs    `convertEmittedRegion` (canonicalize a drifted marker, then `convertToDataRegion`, the same    parity-proven importer every wizard category uses). **The STRUCTURE SPINE is that conversion's    precondition, so the prompt states it as a hard requirement** - root `<div class="PREFIX">` holding    `<div class="PREFIX-box">`, that `-box` class ALONE on the element, `PREFIX-mask` around each `#fN`,    `PREFIX-accent`. An unconvertible region keeps the model's own code (honest hand-crafted output,    read-only timeline) and its `bench-editability` findings DEMOTE TO WARNINGS at the end - except when    a modify started from a data-shaped template, where losing the block is a regression and the repair    loop fights it." Code (src/ai/claudeProvider.ts:1294): "const exampleVariant = spec ? variantsFor(spec.category)[0] : undefined;" Assessment: The code selects index zero without a nearest-distance ranking. The verified authoring, conversion and repair policies from this long item were split into rules; the nearest-example claim was excluded.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F7: Lite does call the alternatives entry point and can receive model-authored skin CSS.** Source paragraph 43: "- **Lite must never call** `generateRaw`, `generateAlternatives`, custom code generation,   polish, import conversion, or code repair. No model call may rewrite the compiled code, and   unsupported scope returns a typed explanation, never an automatic expensive fallback." Code (src/ai/claudeProvider.ts:1331): "async generateAlternatives(prompt, context, options) { if (options?.profile === 'lite') return [await this.generate(prompt, context, options)]; ... if (decision.skin) { ... const skinned = await attemptLiteSkin(spec, decision.skin, context, (t) => validateWith(t, options, run));" Assessment: The method delegates Lite to one result instead of refusing it; decision.skin is a bounded model-produced patch applied by attemptLiteSkin/applyPolish. Preserve typed no-coder/no-repair fallback restrictions, not the literal alternatives-call or absolute no-model-code-change bans.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F8: The default result count is not universally three.** Source paragraph 46: "- **Default (`AiSettings.useHarness` true): `generateAlternatives`** - one design-stage call (forced   `emit_design_alternatives`) returns THREE genuinely different directions; each assembles like a single   harness generation. The AI step offers the pick." Code (src/ai/claudeProvider.ts:1331): "if (options?.profile === 'lite') return [await this.generate(prompt, context, options)]; ... specs = (Array.isArray(output.alternatives) ? output.alternatives : []).slice(0, 3); ... if (!results.length) results.push(await this.generate(prompt, context, options));" Assessment: Lite returns one, Pro takes a different pipeline, and the BYO path caps alternatives at three rather than requiring three. The single design-stage request remains an implementation fact; 'returns THREE' is not a guaranteed contract.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F9: Validation ownership and functional-only repair both have exceptions.** Source paragraph 48: "**LIVE.** The provider is UI-free: callers inject `GenerateOptions.validate` (an `SpxValidator`) - the app wires `validateTemplate` + `benchTemplateRuntime` (`src/validation/runtimeBench.ts`: live-iframe lifecycle, field binding, overlap/overflow, doubled-text stress, and the house editability contract). Bench findings are teaching messages that drive repair rounds. A result that still fails is returned WITH its validation attached - surfaced, never auto-applied. **Grounded assemblies get NO repair loop:** one failing its own bench is a platform bug worth surfacing. On the free-form path the editability contract is enforced deterministically first (`convertEmittedRegion`), so repair rounds only fire on FUNCTIONAL findings." Code (src/ai/claudeProvider.ts:912): "function liteValidator(spec: DesignSpec, ctx: GenerateContext | undefined): SpxValidator { const base: SpxValidator = (template) => productionSpxValidator(null, [], { ... })(template); ... } ... { ...options, profile: undefined, validate: liteValidator(candidate, context) }" Assessment: Lite composes and replaces validation internally. Also generateValidated uses `const editabilityBlocks = !!base && !!parseAnimData(base.js);` and `editabilityBlocks ? v.errors : v.errors.filter((e) => e.rule !== EDITABILITY_RULE)`, so losing editable data during modification triggers repair. Keep injected validation for the general harness and the explicit editability exception, not the unqualified claims.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F10: Attachment manifest numbering can diverge from vision blocks.** Source paragraph 59: "The last three ride `GenerateContext.references` as `{asset, use}` and are vision-only. `attachmentSections` builds ONE numbered manifest plus a block per purpose present, and `imageBlocks` sends the pictures in that order, so "attachment 3" means the same picture in the text and the vision blocks. `modifyContent` reuses the same function. The preselect only ever guesses `asset` vs `mood`; `layout` and `plate` are intents no pixel reveals." Code (src/ai/claudeProvider.ts:534): "...ctx.images.map((a, i) => { ... return `  ${i + 1}. ${a.path} ...`; }), ...refs.map((r, i) => `  ${ctx.images.length + i + 1}. ${REFERENCE_TAG[r.use]}`) ... if (typeof asset.data !== 'string') continue; ... parsed.mime !== 'image/svg+xml'" Assessment: attachmentSections numbers every entry while imageBlocks skips non-string payloads and SVG images. Therefore an SVG asset before a raster reference shifts the numbered-text/vision correspondence. The purpose meanings and guessing limits are retained; perfect attachment numbering is not asserted.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F11: Protected marks have an explicit permitted filter exception.** Source paragraph 60: "**The as-is screen (`assetIntegrity.ts`)** is the protection "use it as it is" promises: a design putting a filter, crop, mask, `object-fit: cover`, rounded corners or an uneven scale on a protected picture is REJECTED. It reports through the injected validator (composed in `productionSpxValidator`, beside the safety screen), so a violation reaches the repair loop rather than only the result card. Same honest limit as `safety.ts`: it reads CSS text, not the resolved cascade. **It finds a protected picture by its `<img src>`, so anything that bakes a `src` must run BEFORE it** - a gate that runs before the last deterministic step measures a document nobody will ever see." Code (src/ai/assetIntegrity.ts:170): "if (isPlatformInkKnock(selector, body)) continue;" Assessment: KNOCK_SELECTOR and KNOCK_DECLARATION permit the platform's monochrome brightness(0), optionally invert(1), filter. The paragraph's blanket filter rejection is false. The independently valid src-before-screen ordering is retained.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F12: Seed is not mirrored through refinement prompts.** Source paragraph 62: "**LIVE.** `GenerateContext` carries two more typed inputs, both rendered by `contextText` (so EVERY path reads them, including raw) and mirrored into `modifyContent` and the spec-refine prompt:" Code (src/ai/claudeProvider.ts:656): "const conversation = ctx?.conversation?.length ? ... : ''; const sections = ctx ? attachmentSections(ctx) : []; ... text: `Modify the template below. ... Request: ${prompt}${conversation}${attachments}${adopt} ...`" Assessment: modifyContent renders conversation and attachments, not seed; specRefine similarly renders priorSpec, conversation and attached images but never context.seed. contextFrom also omits seed. Only contextText contains the explicit seed block; the rule records that actual boundary.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F13: Preflight is not implemented for every paid task.** Source paragraph 69: "**Run `npm run bench:preflight -- <models>` before any paid round.** It is free, reaches no network, and answers what the paid runner structurally cannot: given this `.env` and these candidates, what would each arm ACTUALLY serve? `api/_lib/aiBenchPreflight.ts` resolves every arm through the REAL `liteProfile` + task registry (never a model of their rules - a preflight that reimplements the server drifts from it and then certifies runs the server will refuse), and refuses a plan whose arms are overridden, unapproved, unconfigured, or not pairwise distinct. Each of those wasted a real round: they are invisible in the OUTPUT, because a comparison whose arms resolve to one model still produces differences - sampling noise reads as model character." Code (scripts/ai-bench-preflight.mjs:25): "if (!REQUESTED.length || !['lite', 'import-analysis'].includes(TASK)) { console.error('Usage: npm run bench:preflight -- <model,model,...> [--task=lite|import-analysis]'); process.exit(1); }" Assessment: The CLI only supports Lite and import-analysis. The rule scopes preflight to those tasks; it does not claim Pro or every other paid runner can use it.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Prose that no longer matches the code - F14: The absolute CREATE anti-anchoring instruction conflicts with the active coder.** Source paragraph 80: "- **The anti-anchoring rule is absolute: no catalog design code reaches any CREATE prompt.**   `neutralSkeleton.ts` is what the coder arm studies instead. Free coverage:   `e2e/creative-pilot.spec.ts`." Code (src/ai/claudeProvider.ts:1294): "const exampleVariant = spec ? variantsFor(spec.category)[0] : undefined; ... generateValidated(content, context, undefined, options, run, exampleVariant);" Assessment: generateValidated builds systemPrompt(exampleVariant), whose exampleWithAuthoringRegion invokes the catalog variant. neutralSkeletonTemplate is used by creative/spike and the separate Pro custom loop, not all CREATE prompts. Do not migrate the absolute ban.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D1: Handwritten loading preamble.** Source paragraph 1: "Loaded alongside the root AGENTS.md when working in this directory (Claude reads it via this directory's CLAUDE.md import; Codex reads it directly). Keep it accurate. (The VIDEO harness is its own world: src/ai/video + src/video - see the repository map, `docs/ARCHITECTURE.md` ?8.)" Reason: The loading mechanism is already owned by instruction discovery and the contract compiler. This navigation paragraph adds no implementation decision; the video benchmark boundary is retained separately. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D2: Directory inventory and move-sections remedy.** Source paragraph 2: "**Six subdirectories own their own contract** - `lite/`, `pro/`, `spec/`, `importAnalysis/`, `spike/` and `creative/`, each an `AGENTS.md` with a thin `CLAUDE.md` importing it, loaded only when you work in that directory. A section that describes ONE directory belongs there, not here: this file is read in full by every session touching the harness, and `npm run check:shared-instructions` prints how much room is left. When it runs short, MOVE a directory's section into that directory - and if the files it describes are still loose in this folder, moving them into one is the fix, not shorter prose (`lite/` on 2026-08-26 is the worked example)." Reason: The six listed direct contracts exist, but this remedy belongs to the handwritten-chain architecture. The store/compiler now owns scoped delivery, and check:shared-instructions already checks chain size. No directory movement or source editing is needed for this migration. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D3: Per-heading LIVE/EXPERIMENT/RETIRED formatting.** Source paragraph 3: "**Every `##` section states its STATUS in its first line:** **LIVE** (a user reaches it today), **EXPERIMENT** (built, but flagged off or bench-only - no user reaches it), **RETIRED** (kept only because code still names it). A section's status is not derivable from its content, and reading an experiment as current strategy has already cost this project a paid round. Dead ends, what they measured, and the conditions that would revive them are **`docs/AI_ATTEMPTS.md`** - read it before proposing an approach that sounds new." Reason: Formatting every handwritten ## heading is a decision future rule authors will not face in the one-file-per-rule store. The useful instruction to consult AI_ATTEMPTS and distinguish live/experimental/retired work is retained as a scoped rule. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D4: Historical tier-price table.** Source paragraph 15: "| Tier | Stored id | Label | Price target per finished graphic | |---|---|---|---| | Lite | `lite` | NoaCG Lite | **100 graphics per ?1** (~?0.01); measured **$0.00032** | | Pro | `pro` | NoaCG Pro | **~?10 per 100** (~?0.10 each) | | Bring your own key | `custom` | Bring your own key | whatever the user's provider charges |" Reason: The ids and labels remain in AI_TIERS/TIER_OPTIONS, and route targets are recorded in GOALS; both concepts have rules. The measured $0.00032 and euro target arithmetic do not establish current cost after model changes. Keep this as a historical target/receipt, not a loaded price fact; no paid rerun was performed. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D5: Dated customer-credential verification.** Source paragraph 20: "**Verified end to end on real customer credentials 2026-08-14:** Hugging Face (`Qwen/Qwen2.5-Coder-3B-Instruct`, 74 schema-capable routes listed for the token) and Google (`gemini-3.1-flash-lite`), both returning schema-valid objects through the real adapters. HF issues USER ACCESS TOKENS needing the **Inference Providers** permission, never API keys - hence `credentialNoun` and the `HF_TOKEN` variable name." Reason: A past successful call and a past listing of 74 routes do not certify today's availability. settings.ts now offers a different Hugging Face fallback (openai/gpt-oss-120b), so old Qwen test history should not load as selection guidance. credentialNoun's token vocabulary is retained; the historical calls were not rerun. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D6: The old full-digest measurement.** Source paragraph 22: "**LIVE, on the ADAPT route only.** The design stage used to be handed `catalogDigest()` - **430 variants, ~20,300 tokens, one flat list** - and asked to find the right design on the cheapest model in the product, and that chassis choice is the one decision the whole grounded path rests on." Reason: The retrieval implementation now exists in shortlistFor. The former 430-variant/~20,300-token payload explains its origin but is not a decision an editor must remember; ranking and fallback policy have explicit rules. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D7: Structural-check defect story and measurement mechanics.** Source paragraph 39: "- **Kind** (`structuralKindFindings`) - does the assembled variant carry the anchor the intent   promised? Answered by IDENTITY against `spec.variantId`. The defect every other gate was blind to: a   stinger brief routes to the catalog path CORRECTLY, the design stage returns a lower third, and   static validation, the runtime bench and the parts checks all pass - a lower third really does have a   headline and really does sit bottom-left. Every measurement agreed and the user got the wrong   graphic. It reports only when BOTH sides are known. - **Parts** - list data as a textarea, field capacity, states vs machine/steps statically, plus   repeating groups and zone placement measured in a rendered iframe. It DRIVES every text-bearing field   to a sentinel and re-reads the painted frame, because reading the markup for `id="fN"` cannot see a   standings row, ticker item or credits line a runtime BUILDS from one field. It ignores opacity on   purpose: a region the machine reveals in a later step is transparent during the entrance and   perfectly reachable." Reason: structuralKindFindings and structuralIntentFindings now implement kind/parts checks; e2e/creative-routing.spec.ts covers routed output. The old wrong-kind incident and sentinel-walking implementation no longer need a second loaded instruction; the caller's injected-check and severity boundary is retained. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D8: Lite rollout date and nested-contract navigation.** Source paragraph 42: "**LIVE in production since 2026-08-07; quality is the open problem and the deadline plan is `docs/AI_LITE_PLAN.md`.** The catalog-only, one-result profile selected with `GenerateOptions.profile = 'lite'` moved to **`src/ai/lite/AGENTS.md`** (with its thin `CLAUDE.md`), which loads when you work in that directory; the Lite SKIN and its vision judge went with it. Four things about it bind from out here:" Reason: This is rollout history and a pointer to a separately owned contract, not a current implementation decision. The shared compile boundary and Lite refusal policy are recorded independently. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D9: One compile path pinned by a test.** Source paragraph 43: "- **`lite/pipeline.ts` is the ONE grounded compile path**, and `claudeProvider` is built FROM   it - the benchmark runners compile through the identical function, and   `scripts/ai-lite-bench.test.mjs` pins that no second copy exists." Reason: scripts/ai-lite-bench.test.mjs already asserts claudeProvider imports and calls assembleGroundedTemplate/normalizeLiteSpec and does not call specToTemplate/applyDesignAdjustments itself. The gate owns this prohibition; no extra remembered rule is needed. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D10: Old harness win.** Source paragraph 45: "**LIVE.** The 2026-07-17 benchmark proved the harness a clean win on reliability, editability, overlaps and cost (5/5 clean vs the baselines' 3/5, 0 overlaps, ~3x fewer output tokens, fastest)." Reason: A July benchmark result cannot decide current harness value after subsequent prompt/model changes. The compare-before-claiming rule remains binding; this one-time result remains evidence without being loaded. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D11: Why automatic preview makes screening necessary.** Source paragraph 51: "It blocks rather than warns because a generated template is EXECUTED automatically: the runtime bench loads it the moment a result lands, before anyone has looked at it, in an iframe that today shares the app's origin." Reason: runtimeBench still constructs a same-origin srcdoc iframe and runs generated output. That is rationale for the retained blocking screen and honest-limit rules, not a separate instruction; duplicating it adds no protection. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D12: Four-picture-intent introduction.** Source paragraph 57: "**LIVE.** A dropped image carries FOUR unrelated intents wanting opposite treatment, so the user says which; the vocabulary lives in the MODEL layer because VideoProject persists it." Reason: ImagePurpose and splitByPurpose own the vocabulary and persistence location; the asset/reference behavior is explicitly retained in rules. This introductory explanation does not add another decision. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D13: Single-arm benchmark file directory entry.** Source paragraph 67: "- `scripts/ai-bench.mjs` - the single-arm brief bank + review gallery for prompt iteration." Reason: scripts/ai-bench.mjs remains the single-arm runner, but a file inventory belongs in an index. The paid-run and comparison policies carry its relevant obligations. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D14: Retired Creative Mode pilot navigation.** Source paragraph 78: "**RETIRED 2026-08-09 (owner decision): Creative Mode is superseded by NoaCG Pro and is no longer carried as a parallel architecture.** Nothing in the product reaches this code. The pilot's record, what to MINE from it and what removing it would and would not include moved to **`src/ai/creative/AGENTS.md`** (with its thin `CLAUDE.md`), which loads when you work in that directory." Reason: No product caller imports the retired creative pipeline; spike/run.ts still uses its forwarding neutralSkeleton module for bench work. The retired pilot has its own contract and ledger, so its architecture history should not load on ordinary ai edits. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D15: Development routing incident and hand-maintained allowlist prohibition.** Source paragraph 83: "- **A ROUTE IS REACHABLE IN DEVELOPMENT BECAUSE THE TREE SAYS SO, never because a list does.**   `scripts/aiDevPlugin.mjs` mounts the real `api/ai` handlers, and it used to decide which paths   existed from a hand-kept allowlist. That list hid a whole surface three times: the   imported-graphic-analysis task shipped with no entries, hosted Pro was a dev 404 in every dev   server and the entire e2e suite while production served it (so `loadProStatus` read "this   deployment has no Pro" and the door never appeared locally), and `/api/ai/consent` had never been   routed at all. It is gone. `scripts/apiRouteTable.mjs` derives the function table from the api/   tree and applies the deployment's own measured rule - a `[...path].ts` routes exactly ONE segment   - so an unknown NAME is answered by the real dispatcher in its own error vocabulary and an   unroutable DEPTH is refused exactly as the platform refuses it. `npm run check:api-route-depth`   certifies every `/api/` path our client names through that SAME resolver, so the gate and the dev   server cannot hold different opinions. **Every other AI spec mocks these routes at the network   level, which is why none of the three defects was ever caught** - `e2e/ai-dev-routes.spec.ts`   deliberately does not mock, and is the only thing here that proves a route is reachable." Reason: scripts/aiDevPlugin.mjs builds apiFunctionTable from the tree; check-api-route-depth uses the same resolver and e2e/ai-dev-routes.spec.ts verifies unmapped routes without mocks. These mechanisms now enforce reachability. The three old missing-route stories do not require another prose rule. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D16: Paid Creative Mode round introduction.** Source paragraph 88: "Two consecutive PAID Creative Mode rounds (2026-08-02) produced numbers that looked like the model could not design. Both were platform faults:" Reason: This only introduces fixed historical incidents. The enduring obligation to report live harness faults is retained, and the incident details stay in the record. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D17: Encoded structured output incident.** Source paragraph 89: "1. **Structured output refused for being ENCODED, not wrong.** Anthropic tool use returns a nested    array as a JSON string, sometimes re-wrapping the whole tool envelope inside the property    (`{"concepts":"{\"concepts\":[?]}"}`). `api/_lib/aiGateway.ts` schema-checked it, called it    `malformed_response`, and retried to exhaustion. 7 of 8 briefs died with three usable design    directions sitting in every rejected payload. Fixed by running `decodeStructuredOutput` BEFORE    validation - which had also been breaking every BYO-Anthropic user in the product, not just the    bench." Reason: api/_lib/aiGateway.ts now calls decodeStructuredOutput before validateStructuredOutput. The historical seven-of-eight failure rate and old concepts envelope explain that code but are not a future choice; retain them as evidence. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D18: Creative root-collapse incident.** Source paragraph 89: "2. **A style patch giving `.creative-box` `position: absolute` collapsed the root to 0x0.** The    root is absolutely positioned in its zone and sized by its content, and the box is its only    in-flow child. Sonnet did it in 8 of 8 stylesheets (blank frames); qwen in 4 of 8 (content    survives, re-anchors to the collapsed root, drifts to the frame edge). Fixed by    `keepStructureInFlow` in `src/ai/creative/style.ts`." Reason: keepStructureInFlow exists in the retired creative/style.ts path; the active wizard uses pro/language/pipeline.ts. This trap is for a retired pilot already covered by its own contract, not every AI session. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**True but probably dead - D19: Historical human verdict invalidation.** Source paragraph 90: "**The second one retro-invalidated a human verdict.** The owner's blind review that evening judged frames of which half carried the bug, and its "content on the frame edge" note was the bug, not taste. So: **any conclusion drawn from a round must state which platform faults were live during it**, and a round whose harness changed underneath it is not comparable to one that ran before." Reason: The old evening review cannot be reused as current model evidence. Its general lesson is retained in state-which-platform-faults-were-live; the historical anecdote stays in that rule's record. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Quotation correction for report entry 15:** This replaces the corresponding earlier source quotation, whose non-ASCII character was altered by the Windows stdin encoding; classification and reasoning are unchanged. **True but probably dead - D1: Handwritten loading preamble.** Source paragraph 1: "Loaded alongside the root AGENTS.md when working in this directory (Claude reads it via this directory's CLAUDE.md import; Codex reads it directly). Keep it accurate. (The VIDEO harness is its own world: src/ai/video + src/video - see the repository map, `docs/ARCHITECTURE.md` §8.)" Reason: The loading mechanism is already owned by instruction discovery and the contract compiler. This navigation paragraph adds no implementation decision; the video benchmark boundary is retained separately. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Quotation correction for report entry 18:** This replaces the corresponding earlier source quotation, whose non-ASCII character was altered by the Windows stdin encoding; classification and reasoning are unchanged. **True but probably dead - D4: Historical tier-price table.** Source paragraph 15: "| Tier | Stored id | Label | Price target per finished graphic | |---|---|---|---| | Lite | `lite` | NoaCG Lite | **100 graphics per €1** (~€0.01); measured **$0.00032** | | Pro | `pro` | NoaCG Pro | **~€10 per 100** (~€0.10 each) | | Bring your own key | `custom` | Bring your own key | whatever the user's provider charges |" Reason: The ids and labels remain in AI_TIERS/TIER_OPTIONS, and route targets are recorded in GOALS; both concepts have rules. The measured $0.00032 and euro target arithmetic do not establish current cost after model changes. Keep this as a historical target/receipt, not a loaded price fact; no paid rerun was performed. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Quotation correction for report entry 31:** This replaces the corresponding earlier source quotation, whose non-ASCII character was altered by the Windows stdin encoding; classification and reasoning are unchanged. **True but probably dead - D17: Encoded structured output incident.** Source paragraph 89: "1. **Structured output refused for being ENCODED, not wrong.** Anthropic tool use returns a nested    array as a JSON string, sometimes re-wrapping the whole tool envelope inside the property    (`{"concepts":"{\"concepts\":[…]}"}`). `api/_lib/aiGateway.ts` schema-checked it, called it    `malformed_response`, and retried to exhaustion. 7 of 8 briefs died with three usable design    directions sitting in every rejected payload. Fixed by running `decodeStructuredOutput` BEFORE    validation - which had also been breaking every BYO-Anthropic user in the product, not just the    bench." Reason: api/_lib/aiGateway.ts now calls decodeStructuredOutput before validateStructuredOutput. The historical seven-of-eight failure rate and old concepts envelope explain that code but are not a future choice; retain them as evidence. This is a candidate for the owner to retire, not a deletion or a new authoritative rule.

## Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

**Source navigation retained as evidence:** The migrated sections originally named `retrieval.ts` (shortlisting), `claudeProvider.generate` (the general provider run), `structuralIntent.ts` (mode/intent routing), `validation/structuralIntentCheck.ts` (kind and parts checks), `model/imagePurpose.ts` (picture intent), and `spike/tasteCheck.ts` (bench-only taste reports). The rule bodies use the actual implementation identifiers or fully qualified paths; these original relative section labels are retained here rather than promoted to new instructions. The final paragraph's cross-reference is `docs/VERIFICATION.md`: check the instrument before drawing conclusions about its subject.
