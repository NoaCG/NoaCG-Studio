# ai/choose-fallback-entries-successful-real-calls

Rule: `ai/choose-fallback-entries-successful-real-calls`. Recorded 2026-09-07 on `claude/ai-contract-migration` at 31caedac.

Migrated from src/ai/AGENTS.md, paragraph 19 (zero-based blank-line inventory). Checked against code: api/_lib/aiGateway.ts providerFailure handles Google modelUnavailableForAccount; scripts/check-model-ids.mjs checks listings, while settings.ts records call verification for its fallback. Source prose (historical evidence; only the rule above is authoritative): **A LISTING IS NOT AN ENTITLEMENT, and no filter can fix that** (measured on Google, 2026-08-14). `gemini-2.5-flash` and `gemini-2.5-flash-lite` appear in both of Google's own listings, indistinguishable from live models in every published field, and answer 404 "no longer available to new users" on a key created after their retirement. Discovery has nothing to filter on, so the honest place to help is the ERROR: `providerFailure` maps that shape to "not available on this account - the provider lists it but will not serve it". Two consequences worth keeping: a curated fallback id in `settings.ts` must be one VERIFIED by a real call, not merely listed (`gemini-3.1-flash-lite` is, as of that date), and `scripts/check-model-ids.mjs` cannot catch this class at all - it checks presence, and presence is exactly what lies here.
