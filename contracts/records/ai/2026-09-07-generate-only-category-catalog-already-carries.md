# ai/generate-only-category-catalog-already-carries

Rule: `ai/generate-only-category-catalog-already-carries`. Recorded 2026-09-07 on `claude/ai-contract-migration` at 39835021.

A graphic outside those categories would carry no state machine the control layer knows how to drive, so it would generate and then be unplayable. The constraint reads like a limitation to route around and is the opposite: it is what makes a generated graphic a first-class one.
