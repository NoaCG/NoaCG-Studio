# templates/use-logo-capability-sole-authority-whether

Rule: `templates/use-logo-capability-sole-authority-whether`. Recorded 2026-09-07 on `claude/templates-contract-migration` at 0e351ca6.

The original contract records the rationale and constraints below. Source: src/templates/AGENTS.md, lines 589-594.   measurements under them are **`docs/LOGO_SLOT.md`**, and where a mark is PERMITTED at all is   `docs/MARK_CAPABILITY_AUDIT.md`.   - **The capability is the whole answer**, including for a category that used to hard-wire it -     a phantom Logo field the design can never draw is a lie on every generated control page, and     removing it moves every baseline. **A design gating a logo gates its logo CSS on     `logoEnabled` too**, or the default build changes for a slot nobody asked for.
