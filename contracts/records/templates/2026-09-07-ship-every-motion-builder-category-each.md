# templates/ship-every-motion-builder-category-each

Rule: `templates/ship-every-motion-builder-category-each`. Recorded 2026-09-07 on `claude/templates-contract-migration` at 0e351ca6.

The original contract records the rationale and constraints below. Source: src/templates/AGENTS.md, lines 398-413. - the region stays fully PARSEABLE, so the ordinary importer converts it (the segment becomes a step   `dynamic`) - ONE choreography source, no second code path; - the builders survive the conversion and the export untouched (they're outside the markers); - **every builder of a category ships in every template of it**, so swapping the motion preset is a   pure data edit (one `build` name) with nothing outside the markers rewritten; - the speed knob is read through `motionSpeed()`, never the region's `animSpeed`; - a builder takes `(target, opts)` - `opts` is `{speed, ease}` from the interpreter (absent when the   LEGACY emit calls it, so always default), and it may compose other builders (igMotion's count-up   adds the bar growth once the figure lands). Give a `tl.add()` an EXPLICIT position when the phase   has more than one: a segment is zero-advance in the importer's clock but a real child in GSAP's,   so a bare `'-=N'` after one would resolve differently in the two.  Adding a measured motion to another category = add a builder to its runtime + have the preset `tl.add()` it. Do NOT inline measured math in a region: it makes the template unconvertible.  ### The canonical REPEATING-DATA system (dataRuntimes.ts + sportsRuntimes.ts)
