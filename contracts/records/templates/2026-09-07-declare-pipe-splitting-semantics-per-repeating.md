# templates/declare-pipe-splitting-semantics-per-repeating

Rule: `templates/declare-pipe-splitting-semantics-per-repeating`. Recorded 2026-09-07 on `claude/templates-contract-migration` at 0e351ca6.

The original contract records the rationale and constraints below. Source: src/templates/AGENTS.md, lines 427-429. **Which pipe splits a line is a per-shape decision, not a convention** - the schedule board takes the FIRST (a show name may contain a pipe, the time cannot), the stat list takes the LAST (the figure is the short final part, so the label is what may contain one). Say which, in the runtime.
