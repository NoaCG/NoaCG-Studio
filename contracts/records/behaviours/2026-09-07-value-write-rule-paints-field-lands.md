# behaviours/value-write-rule-paints-field-lands

Rule: `behaviours/value-write-rule-paints-field-lands`. Recorded 2026-09-07 on `claude/two-row-set-recipe-fcbe5e` at 0ad078f8.

Designing the bracket's cross-row lookup (the winner of match 1 moving up into match 5, then into the final) on 2026-09-07: the chain dies after one round. Making one layer both a field role and a layer role is refused by recipeBindingGaps ('one layer is picked for two things') and by naming.ts keying roleOf by layer id, and would show a name the operator's box, a saved document and the OGraf return payload do not carry - the posture docs/OGRAF_STATE_IN_FIELDS.md forbids. Recorded in docs/SVG_BEHAVIOUR_PLAN.md section 13.
