# export/validate-output-target-before-export-publish

Rule: `export/validate-output-target-before-export-publish`. Recorded 2026-09-26 on `claude/align-contracts` at cd21b24b.

The root rule named SPX only; the intent covers every target, and it matters where export and output code is written, not in every session.
