# model/browser-indexeddb-holding-database-written-newer

Rule: `model/browser-indexeddb-holding-database-written-newer`. Recorded 2026-09-08 on `claude/a-model-contract` at 684e2bf2.

Extracted from `src/model/AGENTS.md`. This is rule 6 of the root contract (version every persisted format, degrade an unknown version to read-only rather than crashing) applied to the store itself rather than to a document.
