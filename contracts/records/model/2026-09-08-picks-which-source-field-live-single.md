# model/picks-which-source-field-live-single

Rule: `model/picks-which-source-field-live-single`. Recorded 2026-09-08 on `claude/a-model-contract` at 684e2bf2.

From `src/model/AGENTS.md`: the two engines are 'remotion' (the `tsx` field, the default) and 'hyperframes' (the `html` field, runtime in src/video/hyperframes/); records stored before the field load as 'remotion' through videoProject.ts normalize. `createDefaultVideoProject` mints an engine-matched starter composition.
