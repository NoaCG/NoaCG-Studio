# model/numbered-sibling-family-becomes-part-per

Rule: `model/numbered-sibling-family-becomes-part-per`. Recorded 2026-09-08 on `claude/a-model-contract` at 684e2bf2.

From `src/model/AGENTS.md`: numbered sibling families are recognised the same way the quiz's `.<prefix>-option-N` rows are - `.<prefix>-level-N` (the alert category's severity blocks) each become their own part, labelled by the level word they carry.
