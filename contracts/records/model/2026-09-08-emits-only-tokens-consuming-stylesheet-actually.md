# model/emits-only-tokens-consuming-stylesheet-actually

Rule: `model/emits-only-tokens-consuming-stylesheet-actually`. Recorded 2026-09-08 on `claude/a-model-contract` at 684e2bf2.

Extracted from `src/model/AGENTS.md`. The dead-knob failure this prevents is the same one `setIf` prevents in `packets.ts` when a look is applied.
