# model/export-shim-nothing-else-template-contract

Rule: `model/export-shim-nothing-else-template-contract`. Recorded 2026-09-08 on `claude/a-model-contract` at 684e2bf2.

From `src/model/AGENTS.md`, which kept the shim for one landing under docs/WORKFLOW_ARCHITECTURE.md section 5.5, domain row 1. A row that touches the shim collides with that domain row, which is why the migration row that extracted this rule left the file alone.
