# model/lives-model-layer-same-reason-does

Rule: `model/lives-model-layer-same-reason-does`. Recorded 2026-09-08 on `claude/a-model-contract` at 20864d1f.

Extracted from `src/model/AGENTS.md`.

The first draft of this rule also carried the sentence that `guessPurpose` guesses only between `asset` and `mood` because the other two are intents no pixel reveals. `ai/let-preselect-only-require-user-choose` already says exactly that, on the same scope, so both rendered as consecutive bullets in the same loaded file - two instructions about one function, free to diverge on the next change. The near-duplicate gate did not catch it: the pair scores 0.24 against a 0.60 threshold, diluted by this rule's second half about layering. Worth knowing when the threshold is next calibrated - a rule that is a duplicate PLUS one new clause is exactly what dilution hides.
