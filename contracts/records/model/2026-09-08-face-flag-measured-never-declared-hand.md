# model/face-flag-measured-never-declared-hand

Rule: `model/face-flag-measured-never-declared-hand`. Recorded 2026-09-08 on `claude/a-model-contract` at 684e2bf2.

From `src/model/AGENTS.md`: six of the seventeen bundled faces render uneven digits even under `font-variant-numeric: tabular-nums`, which is why `numericFontStack` exists and why a blanket declaration was not the rule (DESIGN_LANGUAGE section 1). Oswald's digits are even at 400 and span 16% of the em at 700, the weight every sport scoreboard sets them at.
