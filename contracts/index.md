<!-- GENERATED by scripts/compile-contracts.mjs from contracts/rules. Do not edit. -->
# Rule index

Every rule in the store, active or retired. A rule a mechanism carries costs the loaded
contracts nothing; `npm run rules -- <path>` lists what applies to a file.

| id | kind | fires | status | scope | rule |
|---|---|---|---|---|---|
| `root/lesson-recorded-rule-goes-evidence-compiled` | invariant | contract | active | `**` | A lesson is recorded with `npm run learn`: the rule goes to `contracts/rules/`, the evidence to `contracts/records/`, and the compiled `.claude/rules/` files are never edited by hand. |
| `root/read-build-own-exit-code-never` | trap | contract | active | `**` | Read a build's own exit code, never a pipeline's: `npm run build > log 2>&1; echo $?`, because piping the build through `tail` or `head` reports the pipe's status and a failing gate reads as green. |
| `templates-versus/versus-design-declares-two-sides-arrive` | invariant | contract | active | `src/templates/versus/**` | A versus design declares `steps: "1"`. |
| `templates-versus/versus-design-registers-prefix-type-carries` | invariant | contract | active | `src/templates/versus/**` | A versus design registers with prefix `versus` and type `fullscreen`, and carries the fixed field contract: `f0`/`f1` team names, `f2` event line, `f3`/`f4` logo filelists that show a visible placeholder mark. |
| `templates-versus/versus-design-self-assembles-markup-way` | rule | contract | active | `src/templates/versus/**` | A versus design SELF-ASSEMBLES its markup, the way a scoreboard does, rather than taking the info-card contract. |
| `templates/accept-ambient-ranking-input-through-separate` | rule | contract | active | `src/templates/search.ts` | Accept ambient ranking input through `BrowseContext`, separate from user-selected `BrowseFilters`. |
| `templates/add-imports-sanctioned-animation-data-importer` | trap | contract | active | `src/templates/**` | Do not add imports from `src/blocks/**` to `src/templates/**`; the sanctioned animation-data and importer seam is not permission for more upward dependencies. |
| `templates/adding-designs-add-only-their-new` | trap | contract | active | `src/templates/**` | When adding designs to `scripts/overflow-baseline.json`, add only their new rows by hand and preserve every existing row. |
| `templates/after-graphic-settles-capture-runner-temporarily` | trap | contract | active | `e2e/**`, `scripts/**` | After a graphic settles in a capture runner, temporarily override permanent `will-change` hints with `addStyleTag('*{will-change:auto !important}')`, wait two animation frames, remove the override, and wait two more frames before capturing. |
| `templates/arm-machine-timers-through-end-entry` | trap | contract | active | `src/templates/shared/animRuntime.ts` | Arm machine timers through `gsap.delayedCall` from a `tl.call` at the end of the entry timeline, never through `setTimeout`. |
| `templates/author-dimensions-resolution-folded-multiply-font` | rule | contract | active | `src/templates/**` | Author dimensions as `calc(Npx * var(--scale))`, with resolution folded into `--scale` by `computeScale`, and multiply font sizes additionally by `var(--type-scale)`. |
| `templates/call-whenever-design-emits-apply-changed` | trap | contract | active | `src/templates/shared/clock.ts`, `src/templates/startingSoon/shared.ts`, `src/templates/gameTimers/shared.ts`, `src/templates/importedDesign/svg.ts` | Call `clockDataUpdated()` from `update()` whenever a design emits `clockRuntimeJs`. |
| `templates/capture-project-brand-every-wizard-create` | rule | contract | active | `src/templates/**`, `src/components/wizard/**` | Capture the project brand on every wizard Create, so graphics made in one project read as siblings. |
| `templates/check-before-placing-machine-bearing-data` | trap | contract | active | `src/templates/shared/animRuntime.ts`, `src/templates/shared/standard.ts` | Check `hasMachineRuntime(js)` before placing machine-bearing data into a saved template. |
| `templates/choose-shared-logo-arrangement-category-place` | taste | contract | active | `src/templates/shared/logoSlot.ts` | Choose the shared logo arrangement by category: place a lower-third mark beside the text in a single-row grid with the design's children in `.{prefix}-lockup`, and keep the stacked header band for other categories. |
| `templates/combine-strict-facets-choices-within-each` | rule | contract | active | `src/templates/search.ts` | Combine strict facets with AND and choices within each facet with OR; let programme format rank results without hiding them. |
| `templates/compute-countdown-time-deadline-every-tick` | rule | contract | active | `src/templates/shared/clock.ts` | Compute countdown time from a deadline and `Date.now()` on every tick rather than decrementing an interval counter. |
| `templates/create-every-category-data-block-through` | rule | contract | active | `src/templates/**` | Create every category with a `NOACG_ANIM` data block through `convertToDataRegion`, using `CategorySpec.dataRegion` for standard assembly and a direct call for self-assembled categories. |
| `templates/declare-fixed-footprints-through-emit-them` | rule | contract | active | `src/templates/**` | Declare fixed footprints through `stageWidth` and emit them through `stageBoxCss` and `stageExtraJs`, including the `--stage-width` marker. |
| `templates/declare-graphic-type-structure-fields-state` | rule | contract | active | `src/templates/types/**` | Declare a graphic type's structure, fields, state groups, default path, and events independently of its look. |
| `templates/declare-logo-support-variant-capability-emit` | rule | contract | active | `src/templates/**` | Declare logo support as a variant capability and emit an optional logo field only when `ResolvedOptions.logoEnabled` is on. |
| `templates/declare-pipe-splitting-semantics-per-repeating` | rule | contract | active | `src/templates/infographics/dataRuntimes.ts`, `src/templates/infographics/sportsRuntimes.ts` | Declare pipe-splitting semantics per repeating-data shape. |
| `templates/declare-variant-build-capabilities-through-logo` | rule | contract | active | `src/templates/**` | Declare a variant's build capabilities through `maxLines`, logo support, `animationPresets`, and `defaultSteps`. |
| `templates/derive-memoize-template-metadata-per-variant` | rule | contract | active | `src/templates/templateMeta.ts` | Derive and memoize template metadata per variant from the compiled schema and shared taxonomy. |
| `templates/derive-programme-relevance-whether-format-pack` | rule | contract | active | `src/templates/templateMeta.ts` | Derive programme relevance from whether a format's pack contains any type matching the design's graphic category. |
| `templates/drop-query-tokens-reach-design-result` | rule | contract | active | `src/templates/search.ts` | Drop query tokens that reach no design from the result AND and return them as `BrowseOutcome.ignored`. |
| `templates/emit-numeric-sibling-bundled-through-only` | rule | contract | active | `src/templates/shared/base.ts`, `src/templates/shared/numerals.ts` | Emit a numeric sibling's bundled `@font-face` through `rootVarsCss` only when the numeric token is declared. |
| `templates/emit-same-es5-interpreter-every-data` | rule | contract | active | `src/templates/shared/animRuntime.ts` | Emit the same ES5 interpreter in every data-driven template, preserving `buildInTimeline`, `buildOutTimeline`, and `revealNextStep` globals. |
| `templates/enforce-occasion-admission-through-limit-occasion` | rule | contract | active | `src/templates/meta.ts`, `src/templates/search.ts` | Enforce occasion admission through `validateTaxonomy()`: limit the occasion vocabulary to eight, require at least three designs per occasion, reject nonexistent variant ids, and require every phrase to resolve to its occasion. |
| `templates/escape-field-text-shared-data-reading` | trap | contract | active | `src/templates/**` | Escape field text at the shared data-reading boundary before it reaches `innerHTML`, using `escapeHtml()` from `ESCAPE_HTML_JS`, including text interpolated into attributes. |
| `templates/expose-applicable-plus-every-shape-token` | rule | contract | active | `src/templates/**` | Expose `--accent`, `--text-color`, `--text-dim`, `--panel-bg`, `--font-heading`, `--scale`, and the applicable `--type-scale`, plus every shape token the stylesheet consumes from `src/model/themeTokens.ts`. |
| `templates/expose-every-meaningful-visible-string-editable` | rule | contract | active | `src/templates/**` | Expose every meaningful visible string as an editable field, including state words, countdown captions, severity labels, and sponsor headings. |
| `templates/express-pixels-reference-1080p-resolution-preserve` | rule | contract | active | `src/templates/**` | Express `stageWidth` in pixels at the reference 1080p resolution. |
| `templates/fit-placed-shrinkable-raster-text-reducing` | rule | contract | active | `src/templates/shared/textFit.ts` | Fit placed shrinkable raster text by reducing font size within its wrapper, never by distorting the typeface, and retain the fifty-five-percent floor. |
| `templates/generate-playout-behaviour-controls-template-machine` | rule | contract | active | `src/templates/**` | Generate playout behaviour and controls from the template's machine and fields, never from a category or group. |
| `templates/give-each-measured-explicit-position-phase` | trap | contract | active | `src/templates/**` | Give each measured `tl.add()` an explicit position when a phase contains multiple segments. |
| `templates/give-every-design-unique-variant-name` | rule | contract | active | `src/templates/**` | Give every design a unique variant name and a unique created-template name. |
| `templates/give-every-new-category-own-thin` | rule | contract | active | `src/templates/**` | Give every new category its own `AGENTS.md` and thin importing `CLAUDE.md` in its first commit. |
| `templates/give-non-wrapping-reveal-mask-flex` | trap | contract | active | `src/templates/**` | Give a non-wrapping reveal mask in a flex row `flex: none`. |
| `templates/guard-every-load-time-dom-rebuild` | trap | contract | active | `src/templates/**` | Guard every load-time DOM rebuild or paint with the DOM-ready pattern. |
| `templates/hide-input-only-data-holders-through` | trap | contract | active | `src/templates/**` | Hide input-only data holders through the shared `DATA_SOURCE_CLASS` and `dataSourceCss` stylesheet rule, never inline `display: none`. |
| `templates/implement-data-measured-motion-named-builders` | rule | contract | active | `src/templates/**` | Implement data-measured motion as named builders outside the marked animation region that measure the DOM and return GSAP objects. |
| `templates/inject-shared-logo-markup-last-child` | trap | contract | active | `src/templates/shared/logoSlot.ts` | Inject shared logo markup as the last child of an unfamiliar design. |
| `templates/keep-discovery-metadata-out-variant-build` | rule | contract | active | `src/templates/contract.ts` | Keep discovery metadata out of the variant's build contract; resolve facets and search through the shared taxonomy, `templateMeta.ts`, and `search.ts`. |
| `templates/keep-exact-token-matches-exact-use` | rule | contract | active | `src/templates/search.ts` | Keep exact token matches exact, and use `wordMatch` only as a fallback for otherwise unreachable tokens, with one-edit or mid-word matches at half weight and one-edit alias misses resolving to that alias. |
| `templates/keep-imported-fonts-template-assets-under` | rule | contract | active | `src/templates/**` | Keep imported fonts as template assets under `fonts/<file>` with a visible `@font-face`, register them through the FontFace API for builder preview, and ship real font binaries in exports. |
| `templates/keep-internal-assembler-routing-never-label` | rule | contract | active | `src/templates/contract.ts`, `src/templates/meta.ts` | Keep `AssemblerId` as an internal assembler and routing id, never a UI label. |
| `templates/keep-machine-less-interpreter-path-verbatim` | rule | contract | active | `src/templates/shared/animRuntime.ts` | Keep the machine-less interpreter path verbatim when no `machine` key exists. |
| `templates/keep-name-cut-straps-hugging-but` | taste | contract | active | `src/templates/lowerThirds/**` | Keep name-cut straps hugging, but allow `stageWidth` when the design's silhouette requires a bounded composition. |
| `templates/keep-packs-pure-configuration-over-type` | rule | contract | active | `src/templates/packs.ts` | Keep packs as pure configuration over the type-by-family matrix, with every reference format assigned to exactly one pack. |
| `templates/keep-rules-describing-only-template-directory` | rule | contract | active | `src/templates/**` | Keep rules describing only one template directory at that directory's scope rather than in the catalog-wide contract. |
| `templates/keep-single-text-fit-system-per` | trap | contract | active | `src/templates/shared/textFit.ts`, `src/templates/importedDesign/**` | Keep a single text-fit system per imported graphic. |
| `templates/keep-state-wording-editable-data-fields` | rule | contract | active | `src/templates/**` | Keep state wording in editable data fields or hidden word sources, separate from the machine's state and events. |
| `templates/keep-structural-intent-resolution-shared-routing` | rule | contract | active | `src/templates/structuralAnchor.ts` | Keep structural intent resolution in `structuralAnchor.ts`, shared by routing and validation, rather than copying it into either consumer. |
| `templates/keep-uploaded-asset-references-under-font` | rule | contract | active | `src/templates/**` | Keep uploaded asset references under `images/<file>` and font references under `fonts/<file>` for the one-folder export layout. |
| `templates/key-each-kit-choice-graphic-type` | trap | contract | active | `src/templates/kit.ts` | Key each kit choice by its graphic type id or `extra:<designId>`, never by a resolved design id or array index. |
| `templates/let-optional-wall-clock-start-time` | rule | contract | active | `src/templates/shared/clock.ts` | Let the optional wall-clock start-time field override duration when filled, and ignore it when empty. |
| `templates/make-empty-logo-slot-leave-both` | trap | contract | active | `src/templates/shared/logoSlot.ts` | Make an empty logo slot leave both shared arrangements unchanged. |
| `templates/make-graphics-project-read-siblings-through` | taste | contract | active | `src/templates/**` | Make graphics in one project read as siblings through the per-family shape, type, and motion tokens in `docs/DESIGN_LANGUAGE.md`. |
| `templates/measure-stage-reserves-layout-dimensions-never` | trap | contract | active | `src/templates/shared/stageFit.ts` | Measure stage reserves from layout dimensions, never visual bounding rects. |
| `templates/measure-staged-designs-across-full-text` | rule | contract | active | `src/templates/**` | Measure staged designs across the full text-size ladder with `stage-fit-sweep`, `type-floor`, and `overflow-sweep`, using `--type-scale s\|m\|l`. |
| `templates/measure-which-families-resolve-whenever-resolving` | trap | contract | active | `src/templates/kit.ts`, `src/templates/packs.ts` | Measure which families resolve whenever re-resolving a pack; do not assume that a passing declared-family cell gate covers the other families. |
| `templates/offer-generic-broadcast-fields-image-files` | rule | contract | active | `src/templates/contract.ts` | Offer `textfield`, `textarea`, `number`, and `filelist` as generic broadcast fields, with image files under `assetfolder: './images/'`. |
| `templates/omit-imported-designs-through-let-each` | trap | contract | active | `src/templates/importedDesign/**` | Omit `--type-scale` from imported designs through `rootVarsCss` with `typeScale: false`. |
| `templates/pin-reserve-height-multicolumn-text-block` | trap | contract | active | `src/templates/shared/stageFit.ts` | Do not pin a reserve height on a multicolumn text block. |
| `templates/pre-hide-press-revealed-layers-using` | rule | contract | active | `src/templates/shared/animRuntime.ts` | Pre-hide press-revealed layers using their first reveal keyframes, falling back to zero opacity, and show or hide the CSS-hidden root through the lifecycle. |
| `templates/preserve-lifecycle-ambient-motion-measured-builders` | rule | contract | active | `src/templates/shared/standard.ts` | Preserve lifecycle `calls`, ambient `loop` motion, and measured `dynamic` builders when converting legacy animation. |
| `templates/process-events-through-serial-queue-apply` | rule | contract | active | `src/templates/shared/animRuntime.ts` | Process `noacgDispatch` events through one serial queue and apply a flat field payload only after its structural guard accepts. |
| `templates/rank-query-named-occasion-above-subtype` | rule | contract | active | `src/templates/search.ts`, `src/templates/meta.ts` | Rank a query's named occasion above subtype, stacking its contribution with category relevance. |
| `templates/read-animation-speed-through-wherever-design` | trap | contract | active | `src/templates/**` | Read animation speed through `motionSpeed()` wherever design-owned runtime or a wrapper needs it. |
| `templates/read-before-adding-design-judge-variety` | taste | contract | active | `src/templates/**` | Read `docs/CATALOG_VARIETY.md` before adding a design, and judge variety from emitted output rather than declared family axes. |
| `templates/read-before-generating-judging-templates-run` | rule | contract | active | `src/templates/**` | Read `docs/DESIGN_LANGUAGE.md` before generating or judging templates, and run the affected category's catalog sweep after template changes. |
| `templates/record-catalog-baselines-same-change-editing` | rule | contract | active | `src/templates/shared/matchClock.ts` | Re-record the catalog baselines in the same change when editing `shared/matchClock.ts`; treat the dependent boards' re-serialization as an expected baseline change. |
| `templates/resolve-chosen-heading-typeface-through-using` | rule | contract | active | `src/templates/**` | Resolve `--font-numeric` from the chosen heading typeface through `numericFontStack`, using even-width heading digits, else its bundled sibling, else a monospaced fallback. |
| `templates/resolve-declared-metadata-order-then-single` | rule | contract | active | `src/templates/meta.ts` | Resolve declared metadata in the order `VARIANT_META[id]`, `TYPE_META[typeId]`, then the single-valued `CATEGORY_DEFAULT_META[category]`. |
| `templates/resolve-kit-contents-centrally-both-pack` | rule | contract | active | `src/templates/kit.ts` | Resolve kit contents centrally in `kit.ts` from both the pack's type-by-family cells and its `extras`. |
| `templates/resolve-through-first-lower-third-variant` | rule | contract | active | `src/templates/defaultTemplate.ts` | Resolve `createDefaultTemplate()` through the first lower-third variant, `lt01`, so the unsaved-project fallback has the same source as the catalog design. |
| `templates/return-complete-result-never-add-limit` | rule | contract | active | `src/templates/search.ts` | Return the complete result from `browseTemplates` and never add a limit argument. |
| `templates/set-serif-figure-columns-all-caps` | trap | contract | active | `src/templates/**` | Set `font-variant-numeric: lining-nums tabular-nums;` on serif figure columns and all-caps labels. |
| `templates/ship-every-motion-builder-category-each` | rule | contract | active | `src/templates/**` | Ship every motion builder for a category in each of its templates so changing a preset only changes the data's `build` name. |
| `templates/ship-staged-designs-type-size-their` | rule | contract | active | `src/templates/**` | Ship staged designs at the type size their CSS declares for their own default words, reserving shrinkage for operator content. |
| `templates/start-catalog-verification-run-battery-prints` | rule | contract | active | `src/templates/**` | Start catalog verification with `npm run catalog:affected` and run the battery it prints, using `--only <ids>` for attributable designs and the whole catalog for shared changes. |
| `templates/store-repeating-operator-content-hidden-textarea` | rule | contract | active | `src/templates/**` | Store repeating operator content in one hidden textarea `lines` field, with one item per line and pipe-separated parts, rather than adding fields per row. |
| `templates/treat-mark-slot-height-sized-band` | taste | contract | active | `src/templates/**` | Treat a mark slot as a height-sized band with free width, no radius, and no crop. |
| `templates/treat-occasion-alias-admitting-designs-merely` | taste | contract | active | `src/templates/search.ts` | Treat an occasion alias as admitting designs, not merely ranking them. |
| `templates/update-adding-design-keeping-existing-designs` | rule | contract | active | `src/templates/**` | Update `scripts/overflow-baseline.json`, `e2e/catalog-baseline.json`, and `e2e/catalog-render-baseline.json` when adding a design, keeping existing designs unchanged. |
| `templates/use-hugging-footprints-default-lower-thirds` | rule | contract | active | `src/templates/**` | Use hugging footprints by default for lower thirds, corner bugs, end credits, imported designs, tickers, frames, transitions, and versus graphics; declare a fixed stage for other categories. |
| `templates/use-logo-capability-sole-authority-whether` | trap | contract | active | `src/templates/**` | Use the logo capability as the sole authority for whether a design can offer and draw a logo, following `docs/MARK_CAPABILITY_AUDIT.md`. |
| `templates/use-out-direction-entrance-curves-faster` | taste | contract | active | `src/templates/**` | Use Out-direction entrance curves and faster In-direction exits, with Back Out for pops. |
| `templates/verify-shared-marks-both-square-portrait` | rule | contract | active | `src/templates/shared/logoSlot.ts`, `src/templates/lowerThirds/**` | Verify shared marks with both square and portrait artwork through `e2e/catalog/mark-height.spec.ts`. |
| `templates/write-runtime-field-values-through-shared` | rule | contract | active | `src/templates/**` | Write runtime field values through shared `setFieldValue` from `setFieldValueJs`, using text content for text and `src` for images. |
| `wizard/accumulate-erase-marks-each-run-against` | trap | contract | active | `src/components/wizard/import/**` | Accumulate erase marks into `draft.designErases`, each run against the artwork as it stands, and REPLAY the survivors from `draft.designOriginal` when one is removed. |
| `wizard/apply-create-sample-data-reset-new` | invariant | contract | active | `src/components/wizard/CreationWizard.tsx` | Apply on create with sample data reset, so a new project starts from ITS OWN field defaults rather than whatever the previous document held. |
| `wizard/archive-current-result-before-recording-new` | trap | contract | active | `src/components/wizard/steps/ai/**` | Archive the current result BEFORE recording the new request, and record the request even when the box was empty and the brief came from the talk. |
| `wizard/ask-graphic-type-question-dropdown-carrying` | invariant | contract | active | `src/components/wizard/steps/BrowseStep.tsx` | Ask the graphic-TYPE question with ONE dropdown carrying BOTH levels: the shelves as SELECTABLE heading rows over `browsableGroups`, their member categories NBSP-indented under them, each row with its live count. |
| `wizard/ask-pro-tier-thing-which-graphic` | invariant | contract | active | `src/components/wizard/steps/ai/**` | Ask the Pro tier ONE thing: which graphic types the design language is rendered as, every box ticked by default and the LAST tick not removable. |
| `wizard/branch-pro-ending-size-set-never` | rule | contract | active | `src/components/wizard/steps/ai/**` | Branch the Pro ending on the SIZE of the set, never the tier: a set of more than one finishes through the kit ending into a production, and each member is renamed for its type because that name is the export slug and the playout folder an operator reads. |
| `wizard/cancel-pending-lifecycle-demo-timers-debounced` | trap | contract | active | `src/components/wizard/WizardPreview.tsx` | Cancel pending lifecycle-demo timers when a debounced srcdoc commits, push field values from a latest-template ref, and gate the auto-entrance on the document fonts being ready, capped. |
| `wizard/caption-browse-card-name-alone-say` | rule | contract | active | `src/components/wizard/steps/BrowseStep.tsx` | Caption a Browse card with the NAME alone and say what the graphic IS on the line under it. |
| `wizard/carry-line-read-back-what-will` | rule | contract | active | `src/components/wizard/steps/ai/**` | Carry a one-line read-back of what will actually run on the settings button, so the common case needs no click. |
| `wizard/carry-what-picture-own-upload-card` | rule | contract | active | `src/components/wizard/steps/ai/**`, `src/model/imagePurpose.ts` | Carry WHAT A PICTURE IS FOR on its own upload card - use it as it is, make one like this, take the look and feel, make it work over this - because the purpose is a property of the PICTURE and not of the gesture. |
| `wizard/decide-drag-before-binding-kinds-picture` | rule | contract | active | `src/components/wizard/import/**` | Decide a DRAG before the binding kinds on a picture-filled backplate holding both roles on one marker, so a click ticks the picture and the ladder turns growth off. |
| `wizard/derive-what-counts-graphic-type-setup` | invariant | contract | active | `src/templates/types/graphicType.ts`, `e2e/wizard-setup-fields.spec.ts` | DERIVE what counts as a graphic type setup value, never declare it twice: `setupFields` drops every field an operator event carries as its PAYLOAD, because in this model a pick IS payload, and image fields stay out because their value is an asset path. |
| `wizard/drive-fields-step-variant-declared-capabilities` | rule | contract | active | `src/components/wizard/steps/FieldsStep.tsx`, `src/templates/contract.ts` | Drive the Fields step from the variant declared CAPABILITIES: up to `maxLines` text lines, plus the logo toggle and custom upload on a `logo: optional` design. |
| `wizard/embed-every-font-source-fontpicker-offers` | invariant | contract | active | `src/components/wizard/FontPicker.tsx` | EMBED every font source the FontPicker offers, so playout never depends on the machine fonts and the emitted code never references Google. |
| `wizard/feed-footer-brand-chooser-browse-context` | rule | contract | active | `src/components/wizard/steps/BrowseStep.tsx` | Feed the footer brand chooser into Browse as `brandFamily` CONTEXT, never a filter: the package siblings rank first, no chip appears, Clear-all leaves it alone, and a genuine programme match always outranks it. |
| `wizard/follow-wizard-setup-flow-section-docs` | trap | contract | active | `src/components/wizard/import/**`, `docs/SVG_IMPORT_PLAN.md` | Do not follow the "The wizard is a SETUP flow" section of docs/SVG_IMPORT_PLAN.md. |
| `wizard/gate-every-kit-preview-intersection-give` | trap | contract | active | `src/components/wizard/KitTray.tsx`, `src/components/wizard/steps/BrowseStep.tsx` | Gate every kit preview on intersection, and give `.wz-kit-thumb` `pointer-events: none` so clicking the picture of the graphic you want toggles it. |
| `wizard/give-every-wizard-step-own-history` | invariant | contract | active | `src/components/wizard/CreationWizard.tsx`, `src/app/router.ts` | Give every wizard step its own history entry at `#/new/step/<name>`, NAMED and never indexed, because import mode adds a step that shifts every later index. |
| `wizard/give-kit-door-top-browse-swaps` | invariant | contract | active | `src/components/wizard/steps/BrowseStep.tsx`, `src/components/wizard/KitTray.tsx`, `src/templates/kit.ts` | Give the kit ONE door, at the top of Browse: `.wz-buildmode` swaps the step body between the design grid and KitPicker. |
| `wizard/give-only-step-whose-left-pane` | trap | contract | active | `src/components/wizard/CreationWizard.tsx`, `src/components/wizard/import/**` | Give `.wz-body-working` only to a step whose left pane is a CANVAS. |
| `wizard/give-section-line-info-button-rest` | rule | contract | active | `src/components/wizard/SectionHead.tsx` | Give a section ONE line and an info button for the rest: title, one muted line, and an info holding what it does AND why it exists. |
| `wizard/handle-pro-finding-step-cannot-repair` | rule | contract | active | `src/components/wizard/steps/ai/**` | Handle a Pro finding the step cannot repair by clamping categories to the supported set, demoting spec-field findings to warnings against a fixed contract with no repair loop, and standing refine and fix down, because regenerating is the honest move. |
| `wizard/have-behaviour-pickers-explain-themselves-matcher` | rule | contract | active | `src/components/wizard/import/fieldAutoMap.ts` | Have the behaviour pickers explain themselves from the MATCHER and never from copy: the name under an empty box, the unmatched-count notice and the fill offer all read `words.json` through `matchRole`, a picker label is the role own `label`, and every fill pick carries a reason and one Undo. |
| `wizard/have-export-create-save-library-optional` | rule | contract | active | `src/components/wizard/steps/FinishStep.tsx` | Have Export create, SAVE to the library - not optional - and open ExportWindow OVER the wizard through `keepGalleryOpen`, so closing it returns to the last creation step and the editor is never revealed. |
| `wizard/hide-closed-author-rule-because-browser` | trap | contract | active | `src/styles/wizard-and-dialogs.css`, `e2e/**` | Hide a closed `<details>` with an author rule - `details:not([open]) > *:not(summary) { display: none }` - because the browser own `display: none` on non-summary children loses to ANY author `display`, and the Style step disclosures wrap a flex row. |
| `wizard/hold-browse-cards-strict-info-budget` | rule | contract | active | `src/components/wizard/steps/BrowseStep.tsx` | Hold Browse cards to a strict info budget - category and subtype, top families, field summary from semantics, at most three capability badges, complexity, style family - and put everything the budget excludes one info click away in the card detail panel. |
| `wizard/hold-meant-there-baked-text-draft` | rule | contract | active | `src/components/wizard/import/**` | Hold "it is meant to be there / no baked text" as DRAFT state in `designKeepBakedText`, cleared by marking it and reset by a fresh drop. |
| `wizard/hold-user-key-only-unsaved-password` | invariant | contract | active | `src/components/wizard/steps/ai/**` | Hold a user AI key only in the unsaved password-field state and submit it to the credentials endpoint. |
| `wizard/inject-harness-validator-every-provider-call` | rule | contract | active | `src/components/wizard/steps/ai/**` | Inject the harness validator into every provider call, stream the progress stages into the busy line, show the route badge on the result card, and pass a grounded result spec back on refine so spec-level refinement re-assembles deterministically. |
| `wizard/keep-brand-mark-own-draft-field` | rule | contract | active | `src/components/wizard/CreationWizard.tsx` | Keep the brand mark its own draft field, never one of `importedImages`, and let `draftToOptions` decide PER DESIGN whether it travels: never to a design with no logo slot, and never to a picture slot such as a presenter avatar or cover artwork. |
| `wizard/keep-browse-disclosure-every-width-closed` | rule | contract | active | `src/components/wizard/steps/BrowseStep.tsx`, `src/styles/wizard-and-dialogs.css` | Keep ONE Browse disclosure at every width, closed by default, carrying its active count. |
| `wizard/keep-browse-filter-state-creationwizard-back` | trap | contract | active | `src/components/wizard/steps/BrowseStep.tsx`, `src/components/wizard/CreationWizard.tsx` | Keep Browse filter state in CreationWizard as `browseFilters` so Back returns with filters intact, and pass the setter as a REACT DISPATCH so chip toggles compose as functional updates. |
| `wizard/keep-finish-wizard-branch-carrying-graphic` | invariant | contract | active | `src/components/wizard/steps/FinishStep.tsx`, `e2e/wizard-finish.spec.ts` | Keep Finish the wizard ONE branch, carrying the graphic name from `draft.name` applied inside `buildDraftTemplate` so it reaches the topbar, the Save prefill and the export slug through one path. |
| `wizard/keep-harness-default-renders-three-directions` | rule | contract | active | `src/components/wizard/steps/ai/**`, `e2e/ai.spec.ts` | Keep the harness ON by default. |
| `wizard/keep-legibility-settings-shared-control-viewing` | rule | contract | active | `src/components/wizard/ViewingControls.tsx` | Keep the legibility settings ONE shared control - the viewing-target select and the two size-floor toggles are mirrors of one tri-state and interlock in the component. |
| `wizard/keep-more-control-optional-structured-setup` | rule | contract | active | `src/components/wizard/steps/ai/MoreControlPanel.tsx`, `e2e/ai-more-control.spec.ts` | Keep "More control" OPTIONAL structured setup beside the prompt, editing ONE generation spec across category, data fields, look, fonts and animation. |
| `wizard/keep-prepare-erase-offer-never-applied` | invariant | contract | active | `src/components/wizard/import/**` | Keep Prepare erase an OFFER and never applied pixels. |
| `wizard/keep-smallest-managed-tier-smallest-result` | rule | contract | active | `src/components/wizard/steps/ai/**` | Keep the smallest managed tier smallest: one result, remaining allowance, at most two fields, no image or logo input and no style reference, with provider and model settings, brainstorm, raw mode, alternatives, conversion and code repair all hidden. |
| `wizard/keep-thread-composer-single-transcript-talk` | invariant | contract | active | `src/components/wizard/steps/ai/**` | Keep ONE thread and ONE composer: a single transcript of talk turns plus earlier generations kept whole, each restorable, with restoring archiving whatever it displaces. |
| `wizard/keep-three-deliberate-divergences-rest-wizard` | invariant | contract | active | `src/components/wizard/steps/EntryStep.tsx`, `e2e/wizard-entry-fit.spec.ts` | Keep three deliberate divergences from the rest of the wizard on the Entry step: no kit card, cards act on CLICK rather than radio-plus-Continue, and Blank stays behind Advanced mode. |
| `wizard/keep-three-header-doors-distinct-destinations` | invariant | contract | active | `src/components/wizard/CreationWizard.tsx` | Keep the three header doors distinct destinations: the brand lockup is an `<a href="/">` to the public front page, `wz-home` beside it goes Home, and `wz-new-graphic` after it is the shared NewGraphicButton in shell order. |
| `wizard/keep-three-rules-svg-reader-load` | invariant | contract | active | `src/assets/svgImport.ts` | Keep three rules in the SVG reader load-bearing: a `<tspan>` is a LINE or a KERNED RUN and only the measured GAP tells them apart; hidden layers and `<defs>` or `<symbol>` text are never offered; and outline rows are RANKED by whether the shapes read as type, never filtered out. |
| `wizard/keep-two-pro-package-rules-about` | trap | contract | active | `src/ai/pro/language/graphics.ts`, `src/ai/settings.ts`, `e2e/pro-language.spec.ts` | Keep the two Pro package rules that are not about the door OUTSIDE the wizard step, and mutation-check them in the offline suite. |
| `wizard/keep-whole-import-graphic-capability-inside` | invariant | contract | active | `src/components/wizard/import/**` | Keep the whole Import-graphic capability inside `wizard/import/` - the five steps, `DesignPrepCanvas`, `fieldAutoMap`, its CSS and its draft slice - with `import/index.ts` as the ONLY door into it. |
| `wizard/lay-wizard-out-after-entry-rail` | rule | contract | active | `src/components/wizard/CreationWizard.tsx`, `src/styles/wizard-and-dialogs.css` | Lay the wizard out after Entry as rail, form column, preview. |
| `wizard/lead-entry-hero-landing-page-headline` | rule | contract | active | `src/components/wizard/steps/EntryStep.tsx` | Lead the entry hero with the landing page headline verbatim and a subtitle naming BOTH routes to air - the cloud control panel and every export target, never a sample of three. |
| `wizard/lead-imported-design-animation-step-universal` | rule | contract | active | `src/components/wizard/import/**`, `e2e/motion-presets.spec.ts` | Lead the imported-design Animation step with the UNIVERSAL in and out bank in place of the category four whole-unit presets, which the bank stands in for. |
| `wizard/let-directions-survive-refinement-keep-current` | invariant | contract | active | `src/components/wizard/steps/ai/**`, `src/ai/preferences.ts` | Let the directions SURVIVE a refinement: keep `alternatives` and `originals` as parallel arrays, replace only the selected one on refine, and offer an undo that restores the proposed design without spending a generation. |
| `wizard/let-field-file-never-drew-arrive` | invariant | contract | active | `src/components/wizard/WizardPreview.tsx`, `src/components/wizard/import/**` | Let a field the file never drew arrive as a marquee on the PREVIEW, its box coming back as FRACTIONS of the artwork rect. |
| `wizard/let-geometry-propose-followers-author-edit` | invariant | contract | active | `src/components/wizard/import/**` | Let geometry propose followers and the author edit them: `proposeFollowers` measures the runtime guess on the step render, outermost-first, never a group AND its contents. |
| `wizard/let-odd-last-entry-card-span` | rule | contract | active | `src/components/wizard/steps/EntryStep.tsx`, `src/styles/wizard-and-dialogs.css` | Let an odd last entry card span the row through `.wz-entry-card:last-child:nth-child(odd)` and size to its OWN copy. |
| `wizard/list-saved-brands-name-footer-brand` | rule | contract | active | `src/components/wizard/CreationWizard.tsx` | List SAVED brands by name in the footer brand chooser and show no control at all when there are none - never a disabled one. |
| `wizard/locate-wizard-feedback-button-because-shell` | trap | contract | active | `src/components/wizard/CreationWizard.tsx` | Locate the wizard feedback button by `data-area`, because the shell behind the wizard mounts a second one. |
| `wizard/make-add-production-primary-finish-door` | rule | contract | active | `src/components/wizard/steps/FinishStep.tsx`, `src/components/wizard/WizardConfirm.tsx` | Make "Add to the production" the primary Finish door: it applies with `skipNavigation` and `keepGalleryOpen` exactly as Export does, so the editor never flashes on the way to the rundown, and a failed save leaves the wizard open on Finish to press again. |
| `wizard/make-blank-setup-step-never-immediate` | rule | contract | active | `src/components/wizard/CreationWizard.tsx` | Make blank a setup step, never an immediate default-format create. |
| `wizard/make-create-merged-describe-import-step` | invariant | contract | active | `src/components/wizard/steps/ai/**` | Make Create with AI the MERGED describe and import step: one drop zone accepting images AND an existing template. |
| `wizard/make-kit-look-question-deterministic-transform` | rule | contract | active | `src/components/wizard/kitPlan.ts` | Make the kit look question a deterministic transform over the `:root` contract and nothing else: `kitLookPatch` carries a motion preset only where the target design DECLARES it, and the chosen brand reaches every graphic of the set. |
| `wizard/make-mapsvgfieldsstep-mapping-step-mode-over` | invariant | contract | active | `src/components/wizard/import/**`, `src/assets/svgImport.ts` | Make MapSvgFieldsStep the one mapping step in `svg` mode, over text layers, pictures and OUTLINED-TEXT rows, with a ticked glyph group hidden and a placed line standing in - its box MEASURED on the step own inline render and never the preview iframe. |
| `wizard/measure-growth-default-where-artwork-unambiguous` | rule | contract | active | `src/components/wizard/import/**` | MEASURE the growth default with `proposeBannerGrowth` where the artwork is unambiguous: a banner rectangle whose STACKED bound lines are all start-anchored, with room before the margin, defaults to the whole grow ladder. |
| `wizard/measure-ink-erase-removes-split-lines` | invariant | contract | active | `src/components/wizard/import/**` | MEASURE the ink an erase removes, split into LINES, and seed every field at create from that line own bounds, cap height, top and the edge it was set from - never from the loose rectangle the user drew. |
| `wizard/measure-mark-legibility-warning-own-offscreen` | invariant | contract | active | `src/components/wizard/useMarkLegibility.ts` | Measure the mark-legibility warning on its OWN offscreen frame, because the preview iframe deliberately carries no same-origin access and its pixels cannot be read from the app at all. |
| `wizard/name-proven-design-result-adapted-show` | invariant | contract | active | `src/components/wizard/steps/ai/**`, `src/ai/retrieval.ts` | Name the PROVEN DESIGN a result was adapted from and show what it was chosen between: the design name on the card and the retrieved shortlist under it as MiniPreview cards, on the same chassis as the three directions and a Browse tile. |
| `wizard/name-template-dishonestly-zero-results-offer` | rule | contract | active | `src/components/wizard/steps/BrowseStep.tsx` | Name no template dishonestly on zero results: offer to remove the most limiting filter - computed as the chip whose removal restores the most results - and hand off to Create with AI. |
| `wizard/never-let-anything-wizard-renders-navigate` | trap | contract | active | `src/components/wizard/WizardPreview.tsx` | Never let anything the wizard renders navigate a live frame. |
| `wizard/offer-control-can-change-graphic-front` | invariant | contract | active | `src/components/wizard/**` | Offer a control when it can change the graphic in front of the user, and HIDE it, never grey it, when it cannot. |
| `wizard/offer-fix-these-failing-non-managed` | rule | contract | active | `src/components/wizard/steps/ai/**` | Offer "fix these" on a failing non-managed result as a BUTTON and never an automatic loop, sending the exact validator findings back as the instruction at CODE level. |
| `wizard/offer-only-palette-roles-design-actually` | rule | contract | active | `src/components/wizard/steps/StyleStep.tsx` | Offer only the palette roles `cssPaintsWith` says the design paints with, and let the same question decide its TYPEFACE roles (`FONT_ROLES`), whose overrides are keyed by the BARE variable name like every other one on the step. |
| `wizard/offer-panel-picker-only-shapes-bound` | rule | contract | active | `src/components/wizard/import/**` | Offer in the panel picker only shapes a bound line sits in, drawn OR placed - the pair `svgFitNodes` walks - because the runtime grants any other zero. |
| `wizard/offer-pro-tier-only-where-can` | invariant | contract | active | `src/components/wizard/steps/ai/**`, `e2e/pro.spec.ts` | Offer the Pro tier only where it can actually run - the status answer AND the metering backend - and where that is false make it ABSENT, never a greyed row and never a key request. |
| `wizard/offer-three-entry-cards-template-create` | invariant | contract | active | `src/components/wizard/steps/EntryStep.tsx` | Offer three entry cards - template, Create with AI, Import graphic - in a two-column grid, with the video strip separate and blank behind Advanced mode. |
| `wizard/offer-three-execution-tiers-under-settings` | invariant | contract | active | `src/components/wizard/steps/ai/**` | Offer three execution tiers under one settings panel every tier can reach, defaulting to the managed tier when the server offers it and otherwise to bring-your-own-key. |
| `wizard/offer-too-long-control-ladder-owner` | invariant | contract | active | `src/components/wizard/import/**` | Offer the too-long control as a LADDER in the owner order - wider, wider-then-wrap, wrap, smaller, with shrink LAST - emitting `xy` as two rows on one panel. |
| `wizard/offer-universal-motion-bank-every-category` | invariant | contract | active | `src/components/wizard/steps/AnimationStep.tsx`, `src/components/wizard/draft/template.ts` | Offer the universal motion bank in EVERY category, asking the BUILT TEMPLATE whether it has a unit to move rather than what category it is. |
| `wizard/open-text-step-fields-already-placed` | rule | contract | active | `src/components/wizard/import/**`, `src/assets/suggestFields.ts` | Open the Text step with fields already placed where the artwork has an empty panel, deterministically and with no model call, ONCE and only into an empty step. |
| `wizard/park-graphic-whose-motion-measured-rest` | trap | contract | active | `src/components/wizard/WizardPreview.tsx`, `src/blocks/animData.ts` | Park a graphic whose motion is MEASURED - `hasMeasuredMotion` - at rest instead of playing it, decided from the DATA and never the category. |
| `wizard/propose-brand-never-apply-strip-offers` | rule | contract | active | `src/components/wizard/steps/ai/**`, `src/assets/paletteExtract.ts` | PROPOSE brand, never apply it: the strip offers colours read out of the first uploaded image by deterministic arithmetic with no model call, plus the install saved looks. |
| `wizard/put-format-picker-search-above-kit` | rule | contract | active | `src/components/wizard/steps/BrowseStep.tsx` | Put the format picker and the SEARCH above the kit branch as one box - designs on one side, shows and the graphics a kit can hold on the other - with the facets standing down. |
| `wizard/read-project-format-back-foot-rail` | rule | contract | active | `src/components/wizard/CreationWizard.tsx` | Read the project format back at the foot of the rail for the whole walk while the control that sets it stays in the step that owns it - Browse, AI or blank. |
| `wizard/report-pro-result-card-language-rationale` | rule | contract | active | `src/components/wizard/steps/ai/**` | Report on the Pro result card the language, its rationale, its palette and every divergence the platform recorded, keyed to the template by WeakMap so a restored past result shows its own. |
| `wizard/report-what-measured-result-card-verdict` | invariant | contract | active | `src/components/wizard/steps/ai/**`, `src/validation/readiness.ts` | Report what was MEASURED on the result card, not a verdict: group existing findings into the operator-facing rows and add no checks of your own, so a row can honestly read "not played, so not tested" on the one-shot path. |
| `wizard/resolve-deep-linked-through-hit-apply` | rule | contract | active | `src/components/wizard/CreationWizard.tsx` | Resolve a deep-linked `#/new/<variantId>` through `pendingDesignId` and `variantById`, and on a hit apply the SAME patch a BrowseStep card click applies, jumping straight to Fields in template mode - never creating a project, because Finish stays the only door that does. |
| `wizard/run-layer-hit-test-app-side` | trap | contract | active | `src/components/wizard/WizardPreview.tsx` | Run the layer hit-test APP-SIDE against the pushed rects, because the iframe has no allow-same-origin, and tie-break as the editor canvas does: innermost by depth, then smallest box. |
| `wizard/say-user-own-coding-agent-route` | invariant | contract | active | `src/components/wizard/steps/ai/AgentRouteCard.tsx`, `e2e/ai-tiers.spec.ts` | Say the user OWN CODING AGENT route before any tier and any key: one visible line under the section head, the install lines and the docs link behind a "show me" that opens exactly when the settings sheet opens itself, a pointer FIRST in that sheet, and a clause on the bring-your-own-key hint. |
| `wizard/say-which-layers-travel-never-how` | invariant | contract | active | `src/components/wizard/import/**`, `docs/TEXT_BOX_BINDING.md` | Say WHICH layers travel, never how each one behaves: there is no per-row move-or-grow picker, every listed layer moves, and furniture drawn to the growing panel own two edges stretches by itself without ever being a row. |
| `wizard/send-all-three-finish-doors-through` | invariant | contract | active | `src/components/wizard/steps/FinishStep.tsx`, `src/components/wizard/CreationWizard.tsx` | Send all three Finish doors through `applyDraftProject`, which is what keeps them byte-identical. |
| `wizard/set-aside-word-nothing-catalog-carries` | invariant | contract | active | `src/components/wizard/steps/BrowseStep.tsx`, `src/templates/search.ts` | Set aside a word nothing in the catalog carries and SAY IT BACK: `browseTemplates` returns `ignored` and the count line renders it. |
| `wizard/show-chip-whole-type-answer-naming` | rule | contract | active | `src/components/wizard/steps/BrowseStep.tsx` | Show ONE chip for the whole type answer, naming the narrowest level and clearing both levels. |
| `wizard/show-home-row-full-width-graphics` | rule | contract | active | `src/components/wizard/steps/EntryStep.tsx` | Show the Home row full width, with the Graphics and Productions shortcuts as SIBLINGS of the body button, and only when there is saved work. |
| `wizard/show-money-result-card-prices-codebase` | invariant | contract | active | `src/components/wizard/steps/ai/**`, `src/ai/runStats.ts` | Show NO money on the result card - prices are not in this codebase and a stale one would be believed - and print zero tokens as silence, because "0 tokens" is a measurement claim rather than the absence of one. |
| `wizard/show-page-browse-results-catalog-twelve` | rule | contract | active | `src/components/wizard/steps/BrowseStep.tsx`, `e2e/_browse.ts` | Show a PAGE of Browse results, not the catalog: twelve plus a "Show 12 more", with both numbers stated in the count line. |
| `wizard/snapshot-walk-each-three-appliers-finish` | rule | contract | active | `src/components/wizard/CreationWizard.tsx` | Snapshot the walk as a `FinishedWalk` in each of the three appliers when a Finish door closes the wizard. |
| `wizard/spread-every-handler-patches-make-visible` | trap | contract | active | `src/components/wizard/import/**` | SPREAD `svgStretch` in every handler that patches it. |
| `wizard/stand-footer-quiet-create-project-shortcut` | rule | contract | active | `src/components/wizard/CreationWizard.tsx` | Stand the footer quiet "Create project" shortcut down ON Finish, and offer it from every step before it. |
| `wizard/start-every-detected-text-row-prefix` | invariant | contract | active | `src/components/wizard/import/**`, `e2e/import-svg.spec.ts` | Start EVERY detected text row ON. |
| `wizard/state-amber-hover-every-wizard-shell` | rule | contract | active | `src/styles/wizard-and-dialogs.css` | State the amber hover for every wizard-shell control ONCE, over `.wz-header` and `.wz-dot`, never per button. |
| `wizard/take-every-downstream-number-mode-artwork` | invariant | contract | active | `src/components/wizard/import/**` | Take every downstream number in `design` mode from the artwork INTRINSIC pixel size. |
| `wizard/take-finished-template-mode-through-two` | rule | contract | active | `src/components/wizard/import/**` | Take a finished template in `file` mode through a two-stop rail with the imported template as its own preview and the ordinary Finish doors, skipping Prepare, Text and Animation by having a MODE rather than a branch. |
| `wizard/take-only-assets-design-around-these` | rule | contract | active | `src/components/wizard/steps/ai/**` | Take only the as-is assets into the "design around these with a catalog template" escape, and continue into the images, category and template flow. |
| `wizard/take-shape-size-where-painted-never` | trap | contract | active | `src/assets/svgGeometry.ts` | Take a shape size from where it is PAINTED, never from its attributes: apply every transform down the ancestor chain when building the inventory. |
| `wizard/take-video-mode-straight-instant-create` | rule | contract | active | `src/components/wizard/steps/VideoStep.tsx` | Take video mode straight to an INSTANT create - prompt, generation-engine picker, duration, aspect, fps, transparency and asset upload - seeding the brief as the first chat turn and recording the engine on the project. |
| `wizard/travel-conversation-created-project-report-talk` | rule | contract | active | `src/components/wizard/steps/ai/**`, `src/model/aiThread.ts` | Travel the conversation with the created project: report talk turns up on every change so talk added after the last result is caught, commit them at create, and persist them exactly as the spec is. |
| `wizard/treat-entry-step-height-fixed-budget` | trap | contract | active | `src/components/wizard/steps/EntryStep.tsx`, `e2e/wizard-entry-fit.spec.ts` | Treat the Entry step height as a fixed budget at 1366x768: the cards share one column, so growing one part is paid for out of another. |
| `wizard/treat-import-graphic-setup-flow-second` | invariant | contract | active | `src/components/wizard/import/**` | Treat Import graphic as a SETUP flow, not a second editor. |
| `wizard/treat-shared-facet-never-reads-never` | invariant | contract | active | `src/components/wizard/**` | Treat the shared `ProjectFormatPicker` as not a facet - `browseTemplates` never reads it - so never put it inside the filter drawer. |
| `wizard/use-file-chip-class-filename-never` | trap | contract | active | `src/components/wizard/steps/ai/**`, `src/styles/wizard-and-dialogs.css` | Use `.wz-file-chip` for a filename, never `.wz-fid`, the fixed-width field-id badge. |
| `wizard/wait-layer-answer-surface-exist-driving` | trap | contract | active | `src/components/wizard/WizardPreview.tsx`, `e2e/**` | Wait for a LAYER to answer through `awaitPickable`, not for the surface to exist, when driving the artwork canvas. |
| `wizard/walk-kit-through-same-six-steps` | invariant | contract | active | `src/components/wizard/kitPlan.ts`, `src/components/wizard/KitTray.tsx` | Walk a kit through the SAME six steps a single graphic does, with `KitPlan` making each step one graphic OF A SET, plus KitTray, KitLookStep and KitFinishStep. |
| `wizard/wear-beta-tag-inside-title-both` | rule | contract | active | `src/components/wizard/steps/EntryStep.tsx`, `src/components/wizard/steps/AiStep.tsx` | Wear the Beta tag INSIDE the title of both AI doors, and say it in words on the AI card too: `ai-testing-note` LEADS the hint, inline, so the three-line reserve still holds. |
| `wizard/write-speed-buttons-follow-template-own` | rule | contract | active | `src/components/wizard/steps/AnimationStep.tsx` | Write 0.6, 1 and 1.8 from the speed buttons, and follow the TEMPLATE own durations for the lifecycle demo stop and replay rather than a fixed pair. |
