---
v: 1
scope: src/blocks/templateInsert.ts
kind: invariant
fires: contract
status: active
since: 2026-09-07
record: contracts/records/blocks/2026-09-07-inserts-never-replaces-namespaces-everything-naive.md
---
`insertTemplateGraphic` INSERTS, never replaces, and namespaces everything a naive splice would collide on: the donor class prefix gains a unique suffix (`lower-third-2`), donor fields renumber onto the host's next free `fN` with titles marked by the variant name, the donor `:root` block is re-scoped onto the inserted root `#gfx-*` so custom-property inheritance keeps its palette without retinting the host, and the reset/canvas CSS, the donor runtime scaffold and the donor MACHINE are all dropped. One document, one runtime, one timeline and state system.
